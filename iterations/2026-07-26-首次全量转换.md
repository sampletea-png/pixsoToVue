# 2026-07-26 首次全量转换（参数估计工具箱 16 画板）

## 输入

- Pixso URL / 页面范围：当前文件全部设计（1 页面「参数估计」，16 画板）
- skill 版本：0.1.0

## 结果

- 产出项目路径：`generated-project/`（param-estimation-toolbox，Vite+Vue3+TS+Pinia+Router）
- 转换页面数 / 公共组件数：单页应用（16 画板 → 1 模块 + 5 骨架 + 4 面板 + 5 视图 + 7 弹窗）/ 11 个公共组件 + 3 个 Pinia store
- 验收方式：本机无 Node，无法运行；采用代码-截图逐区域对照（3 审查员）+ 修复（15 项），未做浏览器实测

## 问题清单（哪一步失真 / 误判 / 返工）

| 阶段 | 问题 | 根因 | 当时如何修复 |
| --- | --- | --- | --- |
| 构建期 | collect_design 截图全丢（0/16 PNG） | get_screenshot 返回 image/base64 载荷，初版脚本只解析 text | 改脚本处理三种载荷，已修 |
| 构建期 | scaffold --tokens 被默认模板占位跳过 | 先写默认 tokens.css 再拷贝，目标已存在被跳过 | 改脚本顺序，已修 |
| Phase 1 | 设计稿 0 变量 0 样式，extract_tokens 产出为空 | 脚本只认 Pixso 变量/样式，无 DSL 回退 | AI 手工挖 DSL 高频值派生 tokens（应脚本化，见建议） |
| Phase 2 | DSL 导出不含文本字面量，数值只能靠截图目测 | get_node_dsl(compact) 不含 characters | 分析文档标注；但 Phase 6 仍有编造，见下行 |
| Phase 6 | 敏感度表 7/9 行 mock 数据是编造的，验收才发现 | 实现代理没有逐个从截图读数，自行"合理"生成 | Phase 8 修复时按截图逐格读数重写 |
| Phase 5/8 | AppIcon 缺 warning 图标，弹窗渲染字母占位块 | 组件实现前未汇总所需命名实体（图标名）清单 | Phase 8 补注册 + 新增 --color-caution token |
| Phase 6/8 | 工具栏图标数量/顺序错、指标卡形态错、图例色错等 15 项 | 单代理实现范围偏大，细节失真 | Phase 8 三审查员分区对照 + 集中修复 |
| Phase 8 | 本机无 Node，无法 vue-tsc/vite 实测 | 环境限制 | 静态自查代替，遗留运行时风险 |

## 修订建议

- [ ] **脚本化（优先级 1）**：extract_tokens.py 增加 `--mine-dsl` 回退——无变量/样式时自动扫描 artifacts/raw DSL 统计颜色/字号/圆角频次生成 tokens（本次 AI 手工做的就是这套逻辑，应固化）
- [ ] **规则修订（优先级 2）**：component-inference.md 或 SKILL.md Phase 2 增加硬规则——「数值/文案类 mock 数据必须逐个从截图读取并记入分析文档；读不全标 TODO，禁止编造」；Phase 6 实现时只许使用分析文档中记录的数据
- [ ] **规则修订（优先级 2）**：SKILL.md Phase 5 增加前置步骤——「实现公共组件前先汇总全部命名实体清单（图标名、按钮文案、表头、图例）并从 02-analysis 文档核对」
- [ ] **待验证**：get_node_dsl(simplify=false) 是否返回文本字面量？若是，collect_design 应同时拉取完整 DSL 供数据提取（下次运行时验证）
- [ ] **流程观察**：Phase 6 单代理一次实现 17 个文件范围偏大，15 项失真集中于此；考虑按「骨架→面板→视图→弹窗」分 3-4 个代理并在每个代理完成后立即小验收

## 已应用的修订

（2026-07-26 经用户确认全部应用，skill version 0.1.0 → 0.2.0）

1. **脚本**：`extract_tokens.py` 新增 `--mine-dsl` 回退（无变量/样式时自动启用），从 DSL 挖掘高频颜色/字号/圆角生成 `--color-mined-NN` 等变量，已实测（本次数据可挖出 29 个 tokens）。
2. **文档**：SKILL.md Phase 2 增加「数据硬规则」——mock 数据必须逐个从截图读取记入分析文档，禁止编造，Phase 6 只许使用已记录数据。
3. **文档**：SKILL.md Phase 5 增加前置步骤「命名实体清单」（图标/按钮文案/表头/图例/状态变体先汇总核对）。
4. **文档**：SKILL.md Phase 6 改为「分批实现，批后即验」（骨架→面板→视图→弹窗，禁止单执行单元包揽）。
5. **文档**：SKILL.md Phase 1 补充 --mine-dsl 回退与语义别名约定。
6. 遗留待验证：`get_node_dsl(simplify=false)` 是否含文本字面量，下次运行时验证；验证后决定是否让 collect_design 加拉完整 DSL。

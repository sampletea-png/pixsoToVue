# param-estimation-toolbox

由 pixso-to-vue skill 从 Pixso 设计稿生成。

```bash
npm install
npm run dev
```

## 目录约定

- `src/modules/<m>/` —— 业务模块（页面），私有组件与 store 就近放置
- `src/components/` —— 跨模块公共组件
- `src/stores/` —— 全局共享状态
- `src/styles/tokens.css` —— 设计 tokens（来自设计稿变量/样式）

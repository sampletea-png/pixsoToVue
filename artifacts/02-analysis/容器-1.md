# 02 - 页面分析：容器-1

> 由 AI 在 Phase 2 填写。**一次只分析一个画板**，依据：整页截图 + 逐区域 DSL。
> 数据来源：`artifacts/raw/参数估计/容器-1.png`、`容器-1.dsl.json`、`regions/容器-1/*.dsl.json`

## 基本信息

- 页面 / 画板：容器-1 GUID：148:4287
- 页面角色推断（列表 / 详情 / 表单 / 登录…）：**应用主窗口（Convergence Monitor 工作台，填充态）+ 「Close toolbox」模态确认弹窗**。相对应用骨架的状态差异：主区 Tab = Convergence Monitor（激活）、左侧导航 = Parameters（激活）、左面板与图表/表格均有数据（填充态）、中央弹出关闭确认对话框。置信度：高

## 区域划分

| # | 区域（DSL 文件名） | 视觉描述 | 语义角色 |
| --- | --- | --- | --- |
| 1 | 容器-1.dsl.json（区域，1424×24） | 顶部通栏：BEH logo + 标题「Param Estimation Toolbox - HAS Behavior」，右侧 3 个窗口控制图标 | 标题栏（窗口栏） |
| 2 | 容器-379.dsl.json（1424×40） | 标题栏下方一行：左侧 5 个图标按钮 +「Language」+ English 下拉；右侧播放/方块图标 | 工具栏 |
| 3 | Tab.dsl.json（h=844，INSTANCE `Tab`，变体 `属性 1=Parameters`） | 最左窄竖栏：4 个图标+竖排文字（Parameters / Ports / Datasets / EstimateResults），Parameters 高亮 | 左侧导航栏 |
| 4 | 容器-350.dsl.json（328×844） | 左侧面板：标题「Parameters」+ 右上角图标按钮，NAME / Current Value 表头 + 9 行参数键值 | 参数列表面板（表单/列表） |
| 5 | 组件集-9.dsl.json（w=1068，变体 `属性 1=Convergence Monitor`） | 主区顶部 5 个页签：Convergence Monitor（下划线高亮）/ Report / Simulation response / Parameter Sensitivity / Manual tuning | 主区 Tab 页签 |
| 6 | 容器-360.dsl.json（1068×376） | 左右两张图表卡片：左「图表1」Cost/Gradient norm 双折线；右「图表2」kp/kI/tau(+5) 多折线 +「Show scaled parameters」勾选框 | 图表区（2 张折线图卡片） |
| 7 | 容器-365.dsl.json（1068×428） | 11 列 × 9 行数据表，第 6 行蓝色高亮（选中态） | 迭代数据表格 |
| 8 | 容器-366.dsl.json（1440×24） | 底部通栏：左「Run stopped｜Iteration: 0｜Step: 0」，右「Algorithm progress」+ 进度条 +「competed Iteration 69」 | 状态栏 |
| 9 | 弹窗.dsl.json（480×168） | 居中浮层：标题「Close toolbox」+ 右上角 ×，警告图标 +「Close the paramter estimation toolbox？」，右下 Cancel / OK | 确认对话框（弹窗） |

## 语义组件推断（逐区域）

| 区域 | 结构特征（来自 DSL） | 推断组件 | 置信度 | 备注/TODO |
| --- | --- | --- | --- | --- |
| 标题栏 | 1424×24 横向条；INSTANCE `LOGO` + 16px 粗体标题文本；右侧 3 个 INSTANCE（refs：`Icon/General/Tab工具栏/minimize`、`/maxmize`、`/close`） | 窗口标题栏 + minimize/maximize/close 按钮 | 高 | 桌面窗口 chrome；Web 实现可保留为应用栏，close 是打开本页弹窗的触发器 |
| 工具栏 | 多个 INSTANCE `Light/button/icon/table-icon/default`（内含 `Icon/General/Tab/file_import`、`file_export`、`Icon/General/View/save`、`Menu/refresh`、`View/run`）+ 文本「Language」+ INSTANCE `Light/Input/drop-down/default`（值 English） | 图标按钮组 + 语言下拉选择器；右侧 run / stop 图标按钮 | 高 | 图标统一占位 + TODO；stop 对应 `table-icon/disable` 变体（截图右侧方块图标） |
| 左侧导航 | INSTANCE `Tab`（组件集 4 变体：Parameters/Ports/Datasets/EstimateResults），每项 = 图标（`Icon/General/Tab/*`，assetType:icon）+ 竖排文本 | 侧边栏菜单（图标+竖排 label），当前项 Parameters | 高 | `<aside>` + 菜单项 `v-for`；竖排文字用 `writing-mode: vertical-rl` |
| 参数面板 | 标题行（`Icon/General/Tab/Parameters` + 「Parameters」+ `table-icon` 图标按钮，内含 `Icon/General/Tab/file_export`）；表头 NAME / Current Value；9 个同构行组件 `组件集 3`（名称+数值） | 只读参数列表（键值表） | 高 | 9 行 `v-for` 渲染；右上角图标按钮语义不明（截图似铅笔=编辑，DSL 图标名为 file_export），TODO |
| 主区 Tab 栏 | INSTANCE `组件集 9`（组件集 5 变体 = 5 个 Tab 名），每项图标+文本，间以 1px 分隔矩形，激活项下划线 | Tabs 页签组件，激活 = Convergence Monitor | 高 | `v-model` 激活态，页内切换非路由 |
| 图表1 卡片 | 卡片标题行（`Icon/General/Tab/Convergence Monitor` + 文本）+ 图例（蓝点 Cost / 红点 Gradient norm）+ 坐标轴刻度文本（0–40 / 0–1.8 / 0–21）+ 矢量折线 | 折线图表（双 Y 轴：Cost 左轴、Gradient norm 右轴，X=Evaluation count） | 高 | 图表用占位 `<div class="chart">` + TODO，或按用户要求引入图表库；坐标刻度为 mock |
| 图表2 卡片 | 同上卡片结构 + 图例 kp/kI/tau + INSTANCE `Light/Input/drop-down/default`（值「+5」）+ INSTANCE `选中/未选中`（复选框组件集）+ 文本「Show scaled parameters」 | 多参数折线图 + 曲线选择下拉 + 复选框 | 高 | 「+5」下拉表示另有 5 条曲线可叠加；复选框当前未勾选 |
| 数据表格 | 按列组织的 11 个列帧（表头 + 9 个单元格），列：Step, Cost, Ki, Kp, Tau, wn, K_brake×5；第 6 行整行蓝色高亮 | 数据表（DataTable），带行选中态 | 高 | DSL 为列优先结构，Vue 实现按行 `v-for` 重构；5 个同名 K_brake 列疑为占位，TODO 待确认真实列名 |
| 状态栏 | 1440×24 横向条；左组文本「Run」「stopped」+ 分隔竖线 +「Iteration: 0」「Step: 0」；右组「Algorithm progress」+ 200×4 双层圆角矩形（灰底 + 蓝色填充）+「competed Iteration 69」 | 状态栏 + 进度条 | 高 | 「competed」为设计稿原文拼写（应为 competed/completed，保留原文）；进度条蓝色层宽=进度值 |
| 确认弹窗 | 480×168 白底浮层：标题行（`LOGO` + 「Close toolbox」+ `Light/icon/16px/close`）+ 内容行（`Icon/General/Status/error` 警告图标 + 文本）+ 按钮行右对齐：灰底「Cancel」(rgba(238,239,241,1))、蓝底白字「OK」(rgba(26,119,253,1))，圆角 4 | Dialog 确认框（secondary Cancel + primary OK + 头部关闭 ×） | 高 | 截图中无暗色遮罩层，弹窗直接浮在图表上；实现时是否加 mask 待确认（默认加半透明遮罩） |

## 复用候选（供 detect_shared + Phase 3 确认）

| 结构 | 本页出现次数 | 疑似跨页复用 | 建议组件名 |
| --- | --- | --- | --- |
| 标题栏（logo+标题+窗口控制） | 1 | 全部 16 画板共有 | `AppTitleBar` |
| 工具栏（图标按钮+语言下拉） | 1 | 全部画板共有 | `AppToolBar` |
| 左侧导航 Tab（组件集，4 变体） | 1 | 全部画板共有 | `SideNav` |
| 主区 Tab 页签（组件集，5 变体） | 1 | 全部画板共有（变体即激活态） | `MainTabs` |
| 底部状态栏 | 1 | 全部画板共有 | `StatusBar` |
| 图标按钮 `Light/button/icon/table-icon` | 工具栏 5+、参数面板 1 | 跨画板（Ports/Datasets 面板也用 table-icon） | `IconButton` |
| 下拉 `Light/Input/drop-down/default` | 2（English、+5） | 跨画板 | `Dropdown` |
| 复选框 `选中/未选中` | 1 | 跨画板（Manual tuning 等） | `Checkbox` |
| 确认弹窗（标题+图标+文案+双按钮） | 1 | 其他弹窗画板（如保存确认）结构同构 | `ConfirmDialog` |
| 参数键值行 `组件集 3` | 9 | 参数/结果列表面板 | `ParamRow` |
| 图表卡片（标题栏+图例+绘图区） | 2 | Convergence/Simulation/Sensitivity 各 Tab | `ChartCard` |

## 交互推断（规则见 references/interaction-inference.md）

| 触发元素 | 推断事件 | 目标（路由/页内状态） | 置信度 | 依据 |
| --- | --- | --- | --- | --- |
| 标题栏 close 图标（×） | click | 打开本页「Close toolbox」弹窗（页内状态 `showCloseDialog=true`） | 高 | 弹窗标题与关闭语义吻合；画板间仅弹窗有无差异=页内状态 |
| 弹窗 Cancel 按钮 | click | 关闭弹窗，返回主界面（`showCloseDialog=false`） | 高 | 文案即事件；确认对话框标准模式 |
| 弹窗头部 × 图标 | click | 同上，关闭弹窗 | 高 | 视觉惯例（对话框关闭按钮） |
| 弹窗 OK 按钮 | click | 确认关闭 toolbox（退出/关闭窗口）；Web 实现默认关闭弹窗 + `TODO: 交互待确认` | 中 | 文案推断，目标行为无对应画板佐证 |
| 主区 Tab 页签（5 项） | click | 页内 Tab 切换 `activeMainTab`（Report / Simulation response / Parameter Sensitivity / Manual tuning） | 高 | 组件集 5 变体=5 个 Tab；交互规则「Tab 页签多画板」 |
| 左侧导航（4 项） | click | 切换左侧/主内容区 `activeNav`（Parameters / Ports / Datasets / EstimateResults） | 高 | 组件集 4 变体；整屏内容替换，似路由，但单窗口应用按页内状态处理（待 detect_shared 确认） |
| 工具栏 run（播放图标） | click | 启动参数估计运行；状态栏 Run stopped → running | 中 | `Icon/General/View/run` 命名 + 状态栏 Run 状态字段 |
| 工具栏 stop（方块图标） | click | 停止运行 | 中 | 与 run 相邻的播放/停止惯例；`table-icon/disable` 变体提示可禁用态 |
| 语言下拉（English） | click | 展开语言选项并切换 UI 语言 | 中 | 「Language」label + drop-down 实例，文案即事件；选项内容未知 |
| 工具栏 file_import / file_export / save / refresh 图标 | click | 导入 / 导出 / 保存 / 刷新 | 低 | 仅图标命名推断，无画板佐证结果态 |
| 图表2「+5」下拉 | click | 选择要叠加显示的参数曲线（另有 5 条可选） | 中 | drop-down 实例 + 图例已有 kp/kI/tau 三条，「+5」即剩余数量 |
| 「Show scaled parameters」复选框 | change | 切换缩放参数曲线显示（`showScaled`） | 高 | 复选框组件 + 文案即事件 |
| 数据表格行 | click | 行选中（第 6 行高亮即选中态，`selectedStep`） | 中 | 截图第 6 行蓝色高亮=典型行选中视觉 |
| 参数面板右上角图标按钮 | click | 编辑或导出参数列表 | 低 | 截图视觉似铅笔（编辑），DSL 图标名为 `file_export`，两者矛盾，TODO |

## 数据识别（mock 数据 → 响应式数据）

| 设计稿中的内容 | 数据字段 | 类型 |
| --- | --- | --- |
| 参数面板 9 行：K_drive 1.025397 / K_drive 0.765554 / C_v 1.025397 / C_roll 0.765554 / tau_drive 1.025397 / tau_brake 0.765554 / v_e'p's 1.025397 / K_ddrive 0.765554 / k_dbrake 1.025397 | `paramList: [{ name, currentValue }]` | Array<{name: string, currentValue: number}>（参数名重复 K_drive×2，疑为占位） |
| 表格 9 行 × 11 列（Step 1–9；Cost 1.234…11.34；Ki 0.1234…0.3456；Kp 0.5432…0.7890；Tau 0.4567…0.678；wn 5.234…5.56；K_brake×5 列 0.9334…0.9334） | `iterationRows: [{ step, cost, ki, kp, tau, wn, kBrake1..5 }]` | Array<object>（数值型 mock） |
| 表格第 6 行高亮 | `selectedStep = 6` | number |
| 图表1 折线（Cost 蓝 / Gradient norm 红，X 0–21，Y 0–40 / 0–1.8） | `costSeries`、`gradientNormSeries` | Array<{x, y}>（图表 mock 数据） |
| 图表2 折线（kp 蓝 / kI 绿 / tau 黄 + 隐含 5 条） | `paramSeries: [{ name, color, points }]`；`extraCurveCount = 5` | Array<object> |
| 图表2 复选框未勾选 | `showScaled = false` | boolean |
| 状态栏「Run stopped」「Iteration: 0」「Step: 0」「competed Iteration 69」+ 进度条 | `runState='stopped'`、`iteration=0`、`step=0`、`completedIteration=69`、`progress` | string / number（文案保留原文拼写） |
| 弹窗标题「Close toolbox」、正文「Close the paramter estimation toolbox？」、按钮 Cancel / OK | 静态文案（i18n 候选）；「paramter」为设计稿原文拼写 | string |
| 语言下拉当前值 English | `language = 'English'` | string |

## 存疑清单

- 弹窗在截图中**无暗色遮罩**，直接浮于图表之上；Phase 6/7 默认加半透明遮罩 + `TODO: 遮罩待确认`。
- 弹窗 OK 的最终行为（退出应用？返回宿主软件？）无画板佐证，默认仅关闭弹窗 + TODO。
- 参数面板右上角图标按钮：截图似「编辑」铅笔，DSL 图标名为 `file_export`，语义待确认（低置信度）。
- 表格 5 个同名 `K_brake` 列、参数面板 2 个同名 `K_drive` 行，疑为设计占位，真实列名/参数名待确认。
- 表格 DSL 为**列优先**结构（每列一个帧含 9 个单元格），实现时按行重构，行选中态需整行联动。
- 「competed Iteration 69」「Close the paramter estimation toolbox？」含原文拼写错误，按原文保留（不擅自纠正）。
- 左侧导航切换是路由还是页内状态：按规则降级为页内状态（低置信度），待 16 画板横向比对后确认。

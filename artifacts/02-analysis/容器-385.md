# 02 - 页面分析：容器-385

> 由 AI 在 Phase 2 填写。**一次只分析一个画板**，依据：整页截图 + 逐区域 DSL。
> 数据来源：`artifacts/raw/参数估计/容器-385.png`、`容器-385.dsl.json`、`regions/容器-385/*.dsl.json`

## 基本信息

- 页面 / 画板：容器-385 GUID：25:437
- 页面角色推断（列表 / 详情 / 表单 / 登录…）：**应用主窗口 + Parameters 模态弹窗（空态）**。相对应用骨架的状态差异：Convergence Monitor Tab 激活、左侧导航 Parameters 激活、Parameters 弹窗打开且尚未选择任何估计参数（"Selected estimation parameters: 0"，内容为引导文案空态）。置信度：高（截图弹窗层 + DSL 中容器-386 为 1280×720 独立浮层，含 close 图标与空态文案）

## 区域划分

| # | 区域（DSL 文件名） | 视觉描述 | 语义角色 |
| --- | --- | --- | --- |
| 1 | 容器-1.dsl.json | 顶部通栏 1424×24：BEH logo + 标题 + 右侧三枚窗口控制图标 | 标题栏（TitleBar） |
| 2 | 容器-379.dsl.json | 顶部通栏 1424×40：一排图标按钮 + "Language" + English 下拉 + 右侧 run/stop 图标 | 工具栏（Toolbar） |
| 3 | Tab.dsl.json | 左侧竖向窄栏（h=844）：Parameters / Ports / Datasets / EstimateResults 图标+文字，Parameters 高亮 | 侧边栏导航（变体组件，属性1=Parameters/Ports/Datasets/EstimateResults） |
| 4 | 容器-350.dsl.json | 左侧 328×844 面板："Parameters" 标题 + 导出图标 + NAME / Current Value 参数表（9 行） | 参数侧面板（Parameters 面板，被弹窗部分遮挡） |
| 5 | 组件集-9.dsl.json | 主区顶部页签条：Convergence Monitor（激活，蓝色下划线）/ Report / Simulation response / Parameter Sensitivity / Manual tuning | 主区 Tab 页签（变体组件集 8，属性1=五个页签名） |
| 6 | 容器-360.dsl.json | 主区上部 1068×376：Cost / Gradient norm 折线图 + 参数收敛图（kp/kI/tau 图例、"+5" 下拉、"Show scaled parameters" 复选框） | 图表区（Convergence Monitor 内容，弹窗右侧露出一条） |
| 7 | 容器-365.dsl.json | 主区下部 1068×428：Step / Cost / Ki / Kp / Tau / wn / K_brake 多列数据表，带竖向滚动条 | 迭代历史数据表 |
| 8 | 容器-366.dsl.json | 底部通栏 1440×24："Run stopped / Iteration: 0 / Step: 0" + 右侧 "Algorithm progress" 进度条 + "competed Iteration 69" | 状态栏 |
| 9 | 容器-386.dsl.json | 居中浮层 1280×720（白底圆角）：标题 "Parameters" + 关闭 X、蓝色 "Add parameters" 按钮、居中灰色引导空态文案、底部 "Selected estimation parameters: 0" | **Parameters 模态弹窗（空态）——本画板核心差异层** |

## 语义组件推断（逐区域）

| 区域 | 结构特征（来自 DSL） | 推断组件 | 置信度 | 备注/TODO |
| --- | --- | --- | --- | --- |
| 容器-1 | INSTANCE "LOGO" + 加粗标题文本 + "Light/icon/16px/close"（含 Icon/installer/minize、Icon/installer/close 等 assetType:"icon"） | 标题栏 + 窗口控制按钮（min/max/close） | 高 | 桌面应用外壳；Vue 中三个 icon 占位 + TODO |
| 容器-379 | 多个 "Light/button/icon/table-icon/default" 实例（Icon/General/View/save、file_import、file_export、"+" 等）+ "Light/Input/drop-down/default"（内容 English，带 expand 箭头）+ "Icon/General/View/run" | 图标按钮组 + 语言下拉选择 + Run/Stop 按钮 | 高 | 下拉用 `<select>` 或自定义 Dropdown；图标按钮逐个占位 |
| Tab | 组件集变体（属性1=Parameters/Ports/Datasets/EstimateResults），每项 Icon/General/Tab/* 图标 + 文本，竖向排列 | 侧边栏导航菜单（v-model 激活项=Parameters） | 高 | 建议组件 `SideNav` |
| 容器-350 | 标题 "Parameters" + "Light/button/icon/table-icon/default"（file_export 图标）+ 同构行重复 9 次（组件集 3，NAME + Current Value 两列） | 参数列表面板：`v-for` 数据驱动表格 | 高 | 行数据抽 mock；禁止静态复制 9 行 |
| 组件集-9 | 变体集（属性1=Convergence Monitor/Report/Simulation response/Parameter Sensitivity/Manual tuning），激活项有下划线高亮 | Tabs 页签组件，激活态=Convergence Monitor | 高 | 页内 `v-model`，非路由 |
| 容器-360 | 两组矩形内折线/网格 + 轴刻度文本（0–40、0–1.8、Evaluation count 0–21）+ 图例文本 + "Light/Input/drop-down/default"（"+5"）+ INSTANCE "选中/未选中" + "Show scaled parameters" | 图表占位 ×2 + 参数选择下拉 + 复选框 | 高（图表）；高（复选框/下拉） | 图表用占位 div + TODO（或按用户要求引入图表库） |
| 容器-365 | 表头行（Step/Cost/Ki/Kp/Tau/wn/K_brake）+ 同构数据行重复 + "Light/other/scroll-bar" | 数据表格（`v-for`）+ 滚动容器 | 高 | 9 行可见行数据抽 mock |
| 容器-366 | 左：短文本组 "Run"/"stopped"/"Iteration: 0"/"Step: 0"（竖分隔线间隔）；右："Algorithm progress" + 蓝色填充长条（rgba(26,119,253,1)）+ "competed Iteration 69" | 状态栏 + 进度条 | 高 | 进度条 `<progress>` 或 div 宽度绑定 |
| 容器-386 | 白底圆角大浮层：LOGO + 标题 "Parameters" + "Light/icon/16px/close"；蓝底（rgba(26,119,253,1)）圆角矩形 + 白色居中文字 "Add parameters"（118×24，radius 4）；居中灰色双行引导文本；底部通栏 "Selected estimation parameters: 0" | Dialog（v-model:visible）+ primary 按钮 + 空态 Empty 块 + 计数 footer | 高 | 空态文案原文保留英文；选中数 0 为响应式计数 |

## 复用候选（供 detect_shared + Phase 3 确认）

| 结构 | 本页出现次数 | 疑似跨页复用 | 建议组件名 |
| --- | --- | --- | --- |
| 标题栏（容器-1 结构） | 1 | 全部 16 画板同骨架 | `AppTitleBar` |
| 工具栏（容器-379 结构） | 1 | 全部画板 | `AppToolbar` |
| 侧边导航（Tab 变体集） | 1 | 全部画板 | `SideNav` |
| 主区页签条（组件集-9/组件集 8 变体集） | 1 | 全部画板 | `MainTabs` |
| 状态栏（容器-366 结构） | 1 | 全部画板 | `StatusBar` |
| 图标按钮（Light/button/icon/table-icon/*） | 7+ | 工具栏/面板复用 | `IconButton` |
| 下拉（Light/Input/drop-down/default） | 2（English、"+5"） | 弹窗内预计还有 | `BaseDropdown` |
| 参数表行（容器-350 内组件集 3） | 9 | 其他参数相关画板 | `ParameterRow` |
| 迭代表行（容器-365 数据行） | 9 | Convergence Monitor 各状态画板 | `IterationRow` |
| Parameters 弹窗（容器-386） | 1 | 预计有「填充态」姊妹画板 | `ParametersDialog` |

## 交互推断（规则见 references/interaction-inference.md）

| 触发元素 | 推断事件 | 目标（路由/页内状态） | 置信度 | 依据 |
| --- | --- | --- | --- | --- |
| 弹窗右上 X（Light/icon/16px/close） | click | 页内状态：`parametersDialogVisible = false` | 高 | 关闭图标命名明确；其他画板存在无弹窗状态（元素同位置变化→页内状态） |
| "Add parameters" 按钮 | click | 页内状态：弹窗切换到「参数选择列表」状态（含复选列表 + Apply） | 中 | 空态文案自述流程："Click 'Add parameters', check items in the list, then click 'Apply'"；当前画板无列表/Apply，目标应为姊妹画板或弹窗内状态 |
| 侧边导航 Parameters/Ports/Datasets/EstimateResults | click | 页内状态：切换左侧面板内容（Parameters 面板 ↔ Ports/Datasets/EstimateResults 面板） | 高 | Tab 为变体组件（属性1=四项）；仅局部变化→页内 Tab |
| 主区页签 Convergence Monitor/Report/… | click | 页内状态：切换主区内容，`activeMainTab` | 高 | 组件集 8 变体（属性1=五页签名）；规则：Tab 切换不是路由 |
| Language "English" 下拉 | click | 展开语言选项（页内 dropdown open 态） | 高 | "Light/Input/drop-down/default" 实例 |
| 工具栏 Run 图标（Icon/General/View/run） | click | 启动估计：状态栏 Run stopped→running、进度条推进 | 中 | 图标命名 + 状态栏 "Run stopped"/"Algorithm progress" 存在对应状态；无原型连线 |
| 工具栏 Stop 图标 | click | 停止估计 | 中 | 与 Run 成对出现（截图右上 play/square 两枚） |
| 参数收敛图 "+5" 下拉 | change | 切换图表显示的参数组 | 中 | 下拉实例 + 图例 kp/kI/tau 暗示可选参数更多 |
| "Show scaled parameters" 复选框 | change | 页内布尔：切换图表缩放显示 | 高 | INSTANCE "选中/未选中" 组件即 checkbox |
| 侧面板导出图标（file_export） | click | 导出参数列表 | 低 | 仅图标命名推断，无其他证据 |
| 窗口控制 min/max/close | click | 窗口最小化/最大化/关闭 | 高 | Icon/installer/minize、Icon/installer/close 命名；桌面壳惯例（Vue 还原时可仅作占位） |
| 迭代表行 | click/hover | 行高亮/选中（截图中有一行浅蓝底） | 低 | 截图可见选中行样式，无明确后续行为 |

## 数据识别（mock 数据 → 响应式数据）

| 设计稿中的内容 | 数据字段 | 类型 |
| --- | --- | --- |
| 侧栏参数表 9 行：K_drive/1.025397、K_drive/0.765554、C_v/1.025397、C_roll/0.765554、tau_drive/1.025397、tau_brake/0.765554、v_e'p's/1.025397、K_ddrive/0.765554、k_dbrake/1.025397 | `parameters: [{ name, currentValue }]` | Array\<object\> |
| 迭代表 9 行 × 列（Step 1–9；Cost 1.234/14.11/5.6…；Ki 0.1234…；Kp 0.5432…；Tau 0.4567…；wn 5.234…；K_brake 0.9334…） | `iterations: [{ step, cost, ki, kp, tau, wn, kBrake }]` | Array\<object\> |
| 图表轴/图例：Cost、Gradient norm、Evaluation count、刻度 0–40 / 0–1.8 / 0–21、kp/kI/tau、"+5" | `chartSeries` / `chartOptions`（占位即可） | object |
| 状态栏：Run / stopped、Iteration: 0、Step: 0、Algorithm progress、competed Iteration 69 | `runStatus`、`iteration`、`step`、`progress`、`completedIteration` | string / number |
| 弹窗计数 "Selected estimation parameters: 0" | `selectedCount` | number（0） |
| 弹窗空态文案 "No estimation parameters selected. Click \"Add parameters\", check items in the list, then click \"Apply\" to configure initial values and bounds in the main table." | 空态文案常量 `emptyHint` | string（保留英文原文） |
| 语言 "English" | `language` | string |
| 窗口标题 "Param Estimation Toolbox - HAS Behavior" | `appTitle` | string |

## 存疑清单

- 「Add parameters」点击后的目标形态未在本画板出现：可能是弹窗内切换为带复选框的参数列表 + Apply 按钮（姊妹画板）。Phase 6/7 先按「弹窗内状态切换」默认实现 + `TODO: 交互待确认`。
- 工具栏各图标按钮（save/file_import/file_export/"+"）的具体动作无文案佐证，Phase 7 仅绑定占位事件 + TODO。
- 图表（Cost/Gradient norm、参数收敛）按规则先做占位 `<div class="chart">` + TODO，是否引入图表库待用户确认。
- 迭代表行选中（浅蓝底行）的后续行为不明，默认实现为行选中高亮。
- 窗口控制按钮在 Web 还原中无实际意义，建议保留视觉占位不绑定行为。

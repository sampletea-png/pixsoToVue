# 02 - 页面分析：容器-439

> 由 AI 在 Phase 2 填写。**一次只分析一个画板**，依据：整页截图 + 逐区域 DSL。
> 数据来源：`artifacts/raw/参数估计/容器-439.png`、`容器-439.dsl.json`、`regions/容器-439/*.dsl.json`

## 基本信息

- 页面 / 画板：容器-439　GUID：71:4153
- 页面角色推断：应用主窗口的「双层弹窗叠加态」——Parameters 面板为主，之上叠了两层模态弹窗：外层「Parameters」参数选择弹窗（容器-398，1280×720），内层「1D Array Table Editor - .pitch_model_1.Param」数组表编辑器弹窗（容器-443，1080×676）。主区 Tab 与内容被弹窗大面积遮挡。置信度：高

## 区域划分

| # | 区域（DSL 文件名） | 视觉描述 | 语义角色 |
| --- | --- | --- | --- |
| 1 | 容器-1.dsl.json | 顶部通栏：BEH logo + "Param Estimation Toolbox - HAS Behavior" + 右侧 min/max/close 三图标 | 标题栏（窗口栏） |
| 2 | 容器-379.dsl.json | 标题栏下一行：左侧 5 个图标按钮 + "Language" + English 下拉；右侧 run/stop 两图标按钮 | 工具栏 |
| 3 | Tab.dsl.json | 最左窄条：Parameters / Ports / Datasets / EstimateResults 四个图标项（INSTANCE "Tab"，Parameters 激活） | 左侧导航条 |
| 4 | 容器-350.dsl.json | 左侧面板：标题 "Parameters" + 导出图标；表头 NAME / Current Value；9 行参数 | 参数列表面板（侧栏内容） |
| 5 | 容器-365.dsl.json | 主区大表格：Step / Cost / Ki / Kp / Tau / wn / K_brake×5 共 11 列 × 18 数据行，带纵向滚动条（大部分被弹窗遮挡） | 主区迭代结果数据表 |
| 6 | 容器-397.dsl.json | 空白白色矩形 1068×412，无任何子节点 | 空白占位/遮罩（语义待确认） |
| 7 | 容器-366.dsl.json | 底部通栏：左 "Run stopped / Iteration: 0 / Step: 0"，右 "Algorithm progress" + 进度条 + "competed Iteration 69" | 底部状态栏 |
| 8 | 容器-398.dsl.json | 外层弹窗 1280×720：标题 "Parameters"；Add/Clear parameters；两张 array 卡片（Detailed edit / Remove table）；FIX+PARAMETER+CURRENT+INITIAL/FIXED 参数表；底部 "Selected estimation parameters: 11" | 参数选择弹窗（Dialog） |
| 9 | 容器-443.dsl.json | 内层弹窗 1080×676：标题 "1D Array Table Editor - .pitch_model_1.Param"；Display 下拉 + Select/Deselect/Reset；Bound settings 工具行；左侧 9×5 可编辑网格；右侧 X/Y/Z min/max + Fit to Data + 空图表区 + 彩虹色标 | 1D 数组表编辑器弹窗（Dialog） |

## 语义组件推断（逐区域）

| 区域 | 结构特征（来自 DSL） | 推断组件 | 置信度 | 备注/TODO |
| --- | --- | --- | --- | --- |
| 容器-1 | 横向通栏 1424×24，左 logo+粗体标题，右 3 个 16px 图标（component: Tab工具栏/minimize、maxmize、close） | 窗口标题栏 | 高 | 窗口控制按钮桌面端原型可不实现真实行为 |
| 容器-379 | 1424×40 横条，"Light/button/icon/table-icon" 图标按钮组 + "Language" 文本 + INSTANCE "Light/Input/drop-down/default"（English）；右侧 run（Icon/General/View/run）+ stop 按钮 | 工具栏 + 语言下拉 + 运行控制 | 高 | 图标含义：+（添加）、save、file_import、file_export、refresh 等，统一占位 + TODO |
| Tab | INSTANCE "Tab"（ref 4:1384），内含 Icon/General/Tab/{Parameters,Ports,Datasets,EstimateResults} 四项 | 左侧图标导航（垂直菜单） | 高 | 整体是组件实例，直接复用；激活项=Parameters |
| 容器-350 | 328×844 面板，9 个同构行（组件集 3，每行 name+value 两个文本） | 参数列表（`v-for` 数据驱动） | 高 | 表头 NAME / Current Value；header 右侧 Icon/General/Tab/file_export |
| 容器-365 | 11 列 × 19 行同构单元格 + "Light/other/scroll-bar" | 数据表格（只读迭代结果表） | 高 | 后 5 列列名重复为 K_brake，是设计稿复制痕迹，实际应为不同参数列，TODO 确认列名 |
| 容器-397 | 纯白 FRAME 1068×412，无子节点 | 空白占位/遮罩 | 低 | 无语义内容，Phase 3 可忽略或作弹窗背景板 |
| 容器-366 | 1440×24 通栏，左侧状态文本组（1px 分隔线），右侧进度条（200×4 双矩形：灰底+蓝条 rgba(26,119,253,1)） | 状态栏 + ProgressBar | 高 | 进度条蓝色满宽但左侧显示 stopped，属设计稿状态不一致，按响应式数据实现 |
| 容器-398/标题 | 1232×40：LOGO 实例 + "Parameters" + "Light/icon/16px/close" | 弹窗标题栏 | 高 | 与 1D 编辑器弹窗标题栏同构 → 抽公共组件 |
| 容器-398/容器385 | 251×24，"Add parameters" / "Clear parameters" 两文本 | 文字按钮×2 | 中 | DSL 未见按钮底色，截图中 "Add param..." 为蓝底主按钮，实现时按截图样式 |
| 容器-398/容器437 | 1232×64 两行同构：array 名 + "1D array · 50 · 0/50 selected" + " Detailed edit " + " Remove table " | array 卡片列表（`v-for`），各含两个按钮 | 高 | "Detailed edit" 是打开 1D 编辑器的入口 |
| 容器-398/容器365 | 1232×516 表格：ACTION 列（删除图标）、FIX 列 9 个 INSTANCE "选中/未选中"（复选框）、PARAMETER/CURRENT/INITIAL/FIXED 列，9 行 | 参数选择表格（复选框 + 数据行，`v-for`） | 高 | FIX=是否纳入估计；INITIAL/FIXED 列显示 0.6/0.8 等值 |
| 容器-398/底部 | "Selected estimation parameters: 11" | 弹窗状态栏文本 | 高 | 响应式计数 |
| 容器-443/标题 | LOGO + "1D Array Table Editor - .pitch_model_1.Param" + close | 弹窗标题栏 | 高 | 标题含目标 array 名 → 动态拼接 |
| 容器-443/容器3 | "Display" label + 136×24 灰底圆角框（含箭头图标实例 "icon图标箭头"）+ 三个灰底圆角文本按钮（Select all / Deselect all / Reset）+ 右侧提示文本 | 下拉选择 + 次按钮×3 + 静态提示 | 高 | 下拉当前值 "initial Value"；提示 "Right-click cells to select or deselect" |
| 容器-443/容器63 | "Bound settings" label + 分段控件（容器385：蓝底高亮 "Fixed value" + 无底色 "Relative to the initial"）+ 分隔线 + 白底按钮 "Lower bound"/"Upper bound" + 蓝底主按钮 "Apply selected" | SegmentedControl + 按钮组 | 高 | 分段控件激活态=Fixed value（rgba(26,119,253,1)） |
| 容器-443/容器441 | 480×516 网格：Y Value 行头列（1–9+，斑马纹，第 6 行 rgba(230,238,255) 高亮）+ 5 数据列（表头 1–5），每单元格 70×24 白底描边框内嵌文本 "0"；带滚动条 | 可编辑二维网格（表格 + 单元格 input） | 高 | 单元格 = 数字输入框；行 6 高亮=选中态；下方空行是占位，按数据驱动 |
| 容器-443/容器444 | 544×68：3 组同构（X/Y/Z 各 label + min/max 两个 122×24 输入框，值均为 "1"），1px 分隔线，右侧蓝底 "Fit to Data" 按钮 | 坐标范围表单（label+InputNumber×6）+ 主按钮 | 高 | 聚合为 `<form>` 语义 |
| 容器-443/容器446 | 544×432 灰底空区域 + 右侧 24×180 竖向彩虹渐变条（LINEAR 渐变 红→黄→绿→蓝，圆角 4） | 热力图预览区（占位）+ colorbar 图例 | 中 | 图表区无数据，`<div class="chart">` 占位 + TODO；colorbar 用 CSS 渐变还原 |

## 复用候选（供 detect_shared + Phase 3 确认）

| 结构 | 本页出现次数 | 疑似跨页复用 | 建议组件名 |
| --- | --- | --- | --- |
| 窗口标题栏（logo+标题+min/max/close） | 1 | 全部 16 画板 | `AppTitleBar` |
| 工具栏（图标组+Language 下拉+run/stop） | 1 | 全部画板 | `AppToolbar` |
| 左侧导航（Tab 实例，4 项） | 1 | 全部画板 | `SideNav` |
| 底部状态栏（Run 状态+进度条） | 1 | 全部画板 | `StatusBar` |
| 弹窗标题栏（LOGO+标题+close，两弹窗同构） | 2 | 所有弹窗画板 | `DialogHeader` |
| 灰底圆角次按钮 / 蓝底主按钮 | 8+ | 所有画板 | `BaseButton`（variants） |
| 白底描边输入框（单元格 70×24、X/Y/Z 122×24） | 50+ | 所有表单/弹窗画板 | `FormInput` / `InputNumber` |
| 下拉（Light/Input/drop-down） | 2（English、Display） | 多画板 | `BaseSelect` |
| 滚动条（Light/other/scroll-bar） | 3 | 多画板 | 可用原生滚动替代 |
| 复选框（选中/未选中 实例） | 9 | 参数选择类画板 | `BaseCheckbox` |

## 交互推断（规则见 references/interaction-inference.md）

| 触发元素 | 推断事件 | 目标（路由/页内状态） | 置信度 | 依据 |
| --- | --- | --- | --- | --- |
| 外层弹窗 "Detailed edit" 按钮 | click | 页内状态：打开 1D Array Table Editor 弹窗（本画板即为打开后状态） | 高 | 按钮文案 + 内层弹窗标题 "1D Array Table Editor - .pitch_model_1.Param" 与第一张 array 卡片同名；两弹窗局部叠加属页内状态 |
| 两弹窗各自 close 图标（Icon/installer/close） | click | 页内状态：关闭对应弹窗（`v-model:visible`） | 高 | 画板间仅浮层有无差异的模式；close 图标语义明确 |
| 左侧导航 Ports / Datasets / EstimateResults | click | 页内状态：切换左侧面板内容（非路由） | 中 | 应用为单窗口多状态；整屏从不替换，仅面板切换 |
| 网格单元格右键 | contextmenu | 页内状态：选中/取消选中单元格（行 6 高亮为选中态） | 高 | 弹窗内明确提示文案 "Right-click cells to select or deselect" |
| Select all / Deselect all / Reset | click | 页内状态：批量设置单元格选中 / 重置值 | 高 | 文案即事件，与提示文案配套 |
| Display 下拉（initial Value） | change | 页内状态：切换网格显示的数据视图（如 initial/current/bound） | 中 | 下拉组件 + label "Display"，选项内容设计稿未给出 |
| Bound settings 分段控件（Fixed value / Relative to the initial） | click | 页内状态：切换边界模式（激活态高亮移动） | 高 | 分段控件结构 + 单选高亮（Fixed value 蓝底） |
| Lower bound / Upper bound / Apply selected | click | 页内状态：对选中单元格应用下/上边界；Apply selected 提交 | 中 | 文案即事件；具体作用于哪些单元格待确认 |
| X/Y/Z min/max 输入 + "Fit to Data" | click | 页内状态：按数据自动填充 6 个范围输入框 | 中 | 按钮文案即事件；图表区为空，联动效果待确认 |
| 外层弹窗 "Add parameters" / "Clear parameters" | click | 页内状态：添加参数行 / 清空参数表 | 中 | 文案即事件，无后续画板佐证 |
| 外层弹窗 "Remove table" | click | 页内状态：移除对应 array 卡片 | 中 | 文案即事件 |
| 外层弹窗 FIX 复选框 | change | 页内状态：勾选参数纳入估计，联动底部 "Selected estimation parameters: N" 计数 | 高 | 复选框实例名 "选中/未选中" + 底部计数文本 |
| 工具栏 run / stop 图标 | click | 页内状态：启动/停止算法，联动状态栏 "Run stopped/running" 与进度条 | 中 | Icon/General/View/run + 状态栏字段对应；无运行态画板直接佐证 |
| 工具栏 Language English 下拉 | change | 页内状态：切换界面语言 | 低 | 仅见下拉，无其他语言画板 |
| 标题栏 min/max/close | click | 桌面窗口操作 | 低 | Web 原型中一般仅做视觉还原，TODO 不实现 |

## 数据识别（mock 数据 → 响应式数据）

| 设计稿中的内容 | 数据字段 | 类型 |
| --- | --- | --- |
| 左侧参数面板 9 行（K_drive 1.025397、C_v、C_roll、tau_drive、tau_brake、v_e'p's、K_ddrive、k_dbrake…，值 1.025397/0.765554 交替） | `paramList: { name: string; currentValue: number }[]` | mock 数组 |
| 主区表格 11 列 × 18 行（Step 1–9…、Cost 1.234/14.11/5.6…、Ki、Kp、Tau、wn、K_brake×5） | `iterationRows: { step: number; cost: number; ki: number; kp: number; tau: number; wn: number; [param: string]: number }[]` | mock 数组 |
| 外层弹窗参数表 9 行（K_drive、Kbrake、Cv、C_roll、tau_drive、tau_brake、v_eps、K_ddrive、K_dbrake + CURRENT 列 1.025397/0.765554/0.000151/0.209244/0.124356/0.006636/0.021103/-0.000535/0.002793 + INITIAL/FIXED 列 0.6/0.8…） | `estimationParams: { name: string; fixed: boolean; current: number; initial: number }[]` | mock 数组 |
| array 卡片两条（.pitch_model_1.Param「1D array · 50 · 0/50 selected」、.pitch_model_1.Param_1「2D array · 10 x 10 · 0/100 selected」） | `arrayTables: { name: string; dim: '1D'|'2D'; size: string; selected: number; total: number }[]` | mock 数组 |
| "Selected estimation parameters: 11" | `selectedCount: number`（由 `estimationParams` 计算） | 计算属性 |
| 1D 编辑器网格 9 行 × 5 列全 "0"，列头 1–5，行头 1–9 | `gridData: number[][]`（初值全 0） | mock 二维数组 |
| X/Y/Z min/max 输入值全 "1" | `axisRange: { x: {min,max}; y: {min,max}; z: {min,max} }` | mock 对象 |
| Display 下拉当前值 "initial Value" | `displayMode: string` | mock 枚举 |
| Bound settings 激活项 "Fixed value" | `boundMode: 'fixed' \| 'relative'` | mock 枚举 |
| 状态栏 "Run stopped"、"Iteration: 0"、"Step: 0"、"Algorithm progress"、"competed Iteration 69" | `runState: { status: 'stopped'|'running'; iteration: number; step: number; progressText: string }` | mock 对象（"competed" 为设计稿原文 typo，保留） |
| 语言 "English" | `language: string` | mock 枚举 |

## 存疑清单

- 容器-397（纯白 1068×412 空 FRAME）语义不明：疑似弹窗背景遮罩或未完成的占位，Phase 3/6 按背景板处理 + TODO。
- 主区表格后 5 列列名全部重复为 "K_brake"，是设计稿复制粘贴痕迹；真实列名未知，Phase 6 以 `param1…param5` 占位 + TODO。
- 主区顶部 Tab（Convergence Monitor / Report / Simulation response / Parameter Sensitivity / Manual tuning）在本画板被外层弹窗完全遮挡，区域 DSL 中未见，需结合其他画板确认当前激活 Tab。
- "Add parameters" 在 DSL 中无按钮底色，截图中呈蓝底主按钮；样式以截图为准。
- 状态栏进度条蓝色满宽与 "Run stopped" 状态矛盾，按响应式数据实现，不硬编码。
- 1D 编辑器右侧图表区（容器-446）为空灰底 + colorbar，数据驱动方式（热力图？）待确认，占位 + TODO。

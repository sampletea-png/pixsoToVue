# 02 - 页面分析：容器-10

> 由 AI 在 Phase 2 填写。依据：整页截图 + 逐区域 DSL。
> 数据来源：`artifacts/raw/参数估计/容器-10.png`、`容器-10.dsl.json`、`regions/容器-10/*.dsl.json`

## 基本信息

- 页面 / 画板：容器-10  GUID：21:2066
- 页面角色推断：同一应用窗口的「Manual tuning」主 Tab 激活、填充态、无弹窗。这是应用主窗口的一个状态，而非独立页面：左侧面板为 Datasets（Result #1–#4 列表，Result #4 勾选），主区为 Manual tuning（参数表 + Cost companies + Output curves），运行已结束（状态栏 `Run stopped` / `competed Iteration 69`）。置信度：高

## 区域划分

| # | 区域（DSL 文件名） | 视觉描述 | 语义角色 |
| --- | --- | --- | --- |
| 1 | 容器-1.dsl.json | 顶部通栏（1424×24）：BEH logo + 标题 ` Param Estimation Toolbox - HAS Behavior`，右侧三个窗口控制图标 | 标题栏（窗口栏） |
| 2 | 容器-379.dsl.json | 顶部第二通栏（1424×40）：左侧一排图标按钮 + `Language` + English 下拉，右侧 Run/Stop 图标按钮 | 工具栏 |
| 3 | Tab.dsl.json | 左侧窄栏（INSTANCE "Tab"，h=843）：4 个竖排菜单项（图标+旋转文本），Ports 项高亮 | 侧边栏导航 |
| 4 | 容器-380.dsl.json | 左侧面板（328×843）：标题行 `Datasets` + 表头 NAME/Type/Time + 4 行结果列表 | 数据列表面板 |
| 5 | 组件集-9.dsl.json | 主区顶部 Tab 栏（1068 宽）：5 个页签，Manual tuning 下划线激活 | 主区 Tab 页签 |
| 6 | 容器-381.dsl.json | 主区上半（1068×364）：两个 `Result#1` 下拉 + Reset/Evaluate/Save 按钮行 + 参数表格（9 行×5 列，tau_brake 行高亮，含滚动条） | 表单/表格（手动调参面板） |
| 7 | 容器-382.dsl.json | 左下卡片（528×440）：`Cost companies` 标题 + 子 Tab（Total Cost/Dataset/Output）+ 4 行代价值 | 统计列表卡片 |
| 8 | 图表1.dsl.json | 右下卡片（536×440）：`Output curves` 标题 + 两个下拉 + 三系列图例 + 折线图 | 图表卡片 |
| 9 | 容器-366.dsl.json | 底部通栏（1440×24）：左 `Run stopped / Iteration: 0 / Step: 0`，右 `Algorithm progress` + 进度条 + `competed Iteration 69` | 状态栏 |

## 语义组件推断（逐区域）

| 区域 | 结构特征（来自 DSL） | 推断组件 | 置信度 | 备注/TODO |
| --- | --- | --- | --- | --- |
| 容器-1 | 24px 通栏，LOGO + 标题文本 + `Icon/installer/minize`、`Icon/installer/close`、`Icon/General/Tab工具栏/minimize|maxmize|close` | 标题栏 + 窗口控制按钮组 | 高 | 桌面应用自绘窗口栏；图标用占位 |
| 容器-379 | 40px 通栏；`Light/button/icon/table-icon/default|disable` 多个、`icon/16px/+`、`Light/icon/16px/expand`、`icon图标/16px/关闭`、`Light/Input/drop-down/default` ×2、`Icon/General/View/save` | 工具栏：图标按钮组 + Language 下拉 + Run/Stop 按钮 | 高 | 各图标按钮的具体语义（新建/打开/保存等）待确认 |
| Tab | INSTANCE "Tab"（componentSetRef 4:1383），4 个 `Icon/General/Tab/{Parameters,Ports,Datasets,EstimateResults}` + 竖排文本 | 侧边栏导航菜单（图标+文本，激活态高亮） | 高 | 本画板高亮项为 Ports（截图浅蓝底+蓝色左条） |
| 容器-380 | 面板标题行（刷新图标 + `Datasets` + 删除图标）；表头 NAME/Type/Time；4 个同构行（`选中/未选中` 复选框变体 + 名称 + Auto + 时间 + 行尾 view/delete 图标按钮） | 可勾选数据列表（v-for 驱动） | 高 | 行尾图标：`Delete` 命名明确；另一枚为「查看/定位」类，语义待确认 |
| 组件集-9 | INSTANCE（componentSetRef 4:1251），含 5 个 `属性 1={Convergence Monitor,Report,Simulation response,Parameter Sensitivity,Manual tuning}` 变体 + 对应 `Icon/General/Tab/*` | Tab 页签组（v-model 激活态） | 高 | 截图 Manual tuning 有下划线 = 激活 |
| 容器-381（工具行） | 两个下拉（`Result#1` 文本，位于容器 374 内）+ `Reset`/`Evaluate`/`Save` 三个按钮（画板 297/298） | 结果选择下拉 ×2 + 按钮组（Reset 次要 / Evaluate 主色 / Save 次要） | 高 | Evaluate 蓝底白字为 primary |
| 容器-381（表格） | 115 个 `Header` 单元格 + `Light/other/scroll-bar`，表头 Parameter/Value/Initial/Lower/Upper，9 行参数，一行（tau_brake）浅蓝高亮 | 参数表格；Value 列在 Manual tuning 语境下疑为可编辑输入 | 表格：高；可编辑：中 | TODO: Value 单元格是否 input 待确认；行高亮=当前选中行 |
| 容器-382 | 标题 + 等距短文本组（`Total Cost` 下划线激活 / Dataset / Output）+ 4 行「label + 值」 | 子 Tab + 代价指标列表 | 高 | 值为「旧 → 新 (差值)」格式文本 |
| 图表1 | 标题 `Output curves` + 两个下拉（`no_drive_reference_cropped_stop`、`v_hat_kmh`）+ 三圆点图例 + 坐标刻度 + `曲线` 矢量 | 图表卡片：信号选择下拉 ×2 + 图例 + 折线图占位 | 高 | 图表用占位 div + TODO（或按用户指定图表库） |
| 容器-366 | 24px 通栏，左侧 4 段状态文本（`Left` 容器），右侧 `Algorithm progress` + 长条矩形（进度条）+ 完成文本 | 状态栏 + 进度条 | 高 | 纯展示，无交互 |

## 复用候选（供 detect_shared + Phase 3 确认）

| 结构 | 本页出现次数 | 疑似跨页复用 | 建议组件名 |
| --- | --- | --- | --- |
| 标题栏（logo+标题+窗口控制） | 1 | 全部 16 画板 | `AppTitleBar` |
| 工具栏（图标按钮+语言下拉+Run/Stop） | 1 | 全部 16 画板 | `AppToolbar` |
| 左侧导航（4 项图标+竖排文本） | 1 | 全部 16 画板 | `SideNav` |
| 主区 Tab 栏（5 页签） | 1 | 全部 16 画板 | `MainTabs` |
| 状态栏（运行状态+进度条） | 1 | 全部 16 画板 | `StatusBar` |
| Datasets 列表面板 | 1 | 多画板（容器-9 等结构相同） | `DatasetPanel` / `ResultList` |
| 下拉选择（Light/Input/drop-down） | 4（English、Result#1 ×2、图表信号 ×2 计 5 处） | 跨画板 | `AppSelect` |
| 图标按钮（Light/button/icon/table-icon） | 工具栏+列表行内约 12 处 | 跨画板 | `IconButton` |
| 代价指标行 / 参数表行 | 4 / 9 | 页内 v-for | 行内模板即可，不必独立组件 |

## 交互推断（规则见 references/interaction-inference.md）

| 触发元素 | 推断事件 | 目标（路由/页内状态） | 置信度 | 依据 |
| --- | --- | --- | --- | --- |
| 主区 5 个 Tab 页签 | click → 切换激活页签 | 页内状态 `activeTab`（非路由） | 高 | 16 画板为同一窗口不同状态，仅内容区不同；规则「Tab 页签多画板」 |
| 左侧导航 4 项 | click → 切换左侧面板内容 | 页内状态 `activeNav` | 高 | 视觉惯例+跨画板仅面板内容变化 |
| Datasets 列表行复选框 | click → 勾选/取消该结果集 | 页内状态 `selectedIds` | 高 | DSL 存在 `选中/未选中` 两种变体 |
| 列表行尾 Delete 图标 | click → 删除该行 | 页内状态（删除数据项） | 中 | 节点命名 `Delete`，文案即事件 |
| 列表行尾另一图标（圆形箭头状） | click → 查看/载入该结果 | 页内状态（载入到 Manual tuning） | 低 | 无语义命名，仅视觉惯例 |
| 面板头部删除图标 | click → 批量删除勾选项 | 页内状态 | 中 | 与勾选框联动的视觉惯例 |
| `Result#1` 下拉 ×2 | change → 选择载入的估计结果 | 页内状态 `currentResultId` | 中 | 下拉+结果名文本 |
| `Reset` 按钮 | click → 参数重置为 Initial | 页内状态（参数表数据重置） | 中 | 文案即事件；具体重置范围待确认 |
| `Evaluate` 按钮（primary） | click → 用当前手动参数评估/仿真，刷新 Cost 与 Output curves | 页内状态（触发计算+数据刷新） | 中 | 文案即事件；与 Manual tuning 语境吻合 |
| `Save` 按钮 | click → 保存手动调参结果为草稿/新结果 | 页内状态 | 中 | 文案即事件+图例存在 `Manual draft` 系列 |
| 参数表 Value 单元格 | 编辑 → 修改参数值 | 页内状态（表单数据） | 中 | Manual tuning 语义推断；DSL 中单元格为 `Header` 矩形+文本，无 input 实例 |
| Cost companies 子 Tab（Total Cost/Dataset/Output） | click → 切换代价视图 | 页内状态 `costTab` | 高 | 等距短文本组+下划线激活 |
| 图表两个下拉 | change → 切换数据集/信号曲线 | 页内状态 | 中 | 下拉位于图表卡片头部，内容为数据集名/信号名 |
| 工具栏 Run / Stop 图标按钮 | click → 启动/停止估计算法 | 页内状态 `runState`（联动状态栏） | 中 | 状态栏 `Run stopped` 与播放/停止图标对应 |
| `Language` English 下拉 | change → 切换界面语言 | 页内状态 `locale` | 高 | 文案即事件 |
| 窗口控制（minimize/maxmize/close） | click → 窗口最小化/最大化/关闭 | 桌面窗口行为（Web 端可降级为占位） | 高 | `Icon/installer/*` 命名+桌面窗口惯例 |
| 状态栏进度条/文本 | 无交互（纯展示） | — | 高 | 状态栏惯例 |

## 数据识别（mock 数据 → 响应式数据）

| 设计稿中的内容 | 数据字段 | 类型 |
| --- | --- | --- |
| 窗口标题 ` Param Estimation Toolbox - HAS Behavior` | `appTitle` | string |
| 左导航 4 项（Parameters/Ports/Datasets/EstimateResults） | `navItems: [{ key, label, icon }]` | array |
| 主 Tab 5 页签 | `mainTabs: [{ key, label, icon }]`，`activeTab='Manual tuning'` | array + string |
| 结果列表 4 行（Result #1–#4 / Auto / 11:28:34、10:21:54 / 勾选态） | `results: [{ id, name, type, time, checked }]` | array |
| 参数表 9 行（K_drive…K_dbrake / Value / Initial / Lower / Upper） | `params: [{ name, value, initial, lower, upper }]` | array |
| 参数表当前高亮行（tau_brake） | `activeParamRow` | string/index |
| Cost companies 4 行（Total SSE、Fit SSE、Validation SSE、Fit SSE；`59.0466 → 59.0466 (+0)` 等） | `costItems: [{ label, from, to, delta }]` | array |
| 图表数据（三系列 v_hat_kmh Measured/Auto result/Manual draft；轴 28–30.5 / 0–3.5） | `chartSeries`、`chartAxes` | array/object |
| 图表下拉选项（`no_drive_reference_cropped_stop`、`v_hat_kmh`） | `selectedDataset`、`selectedSignal` | string |
| 结果下拉选项（`Result#1` ×2） | `selectedResultA/B` | string |
| 状态栏文本（`Run stopped`、`Iteration: 0`、`Step: 0`、`competed Iteration 69`）+ 进度条 | `runState`、`iteration`、`step`、`progressText`、`progress` | string/number |
| Language 当前值 `English` | `locale` | string |

## 存疑清单

- 左导航高亮项为 Ports，但左侧面板标题是 `Datasets` 且列出的是 Result #1–#4（像估计结果），二者语义不一致；与容器-9 画板对比高亮完全相同，疑为设计稿静态选中态。Phase 6/7 默认按「高亮=选中导航项、面板内容随导航切换」实现 + TODO。
- 参数表 Value 列是否可编辑输入（DSL 无 input 实例，仅 Manual tuning 语境推断）。
- 工具栏左侧各图标按钮（table-icon 系列、`+`、expand、关闭）的具体功能无语义命名，按占位图标按钮实现 + TODO。
- 结果列表行尾「圆形箭头」图标的功能（查看/载入/刷新）不确定。
- `Evaluate`/`Save`/`Reset` 的确切行为与目标数据范围待用户确认。
- 两个 `Result#1` 下拉的差异（对比基准 vs 当前编辑对象？）不确定。

# 02 - 页面分析：容器-391

> 由 AI 在 Phase 2 填写。**一次只分析一个画板**，依据：整页截图 + 逐区域 DSL。
> 数据来源：`artifacts/raw/参数估计/容器-391.png`、`容器-391.dsl.json`、`regions/容器-391/*.dsl.json`

## 基本信息

- 页面 / 画板：容器-391  GUID：35:3075
- 页面角色推断（列表 / 详情 / 表单 / 登录…）：**主应用窗口的「Algorithm 算法设置弹窗打开」状态**。底层主区为 EstimateResults 的 Convergence Monitor Tab（填充态：迭代表格 + 统计条 + 完成消息），左侧面板为 Ports 页；其上叠加模态「Algorithm」设置对话框（表单弹窗）。 置信度：高

## 区域划分

| # | 区域（DSL 文件名） | 视觉描述 | 语义角色 |
| --- | --- | --- | --- |
| 1 | 容器-1.dsl.json | 顶部通栏：BEH logo + 标题「Param Estimation Toolbox - HAS Behavior」+ 右侧 3 个窗口控制图标 | 标题栏（桌面窗口 chrome） |
| 2 | 容器-379.dsl.json | 标题栏下一行：左侧 5 个图标按钮 +「Language」+「English」下拉，右侧 2 个图标按钮（▶ / ■） | 工具栏 |
| 3 | Tab.dsl.json | 最左窄条，4 项图标 + 竖排文本（Parameters / Ports / Datasets / EstimateResults），Ports 高亮 | 左侧导航（竖向菜单） |
| 4 | 容器-350.dsl.json | 左侧 328px 面板：Ports 标题 + 说明文字 + Input ports / Out ports 两组「Fit 复选框 + Port + CSV header」行 | 侧边栏内容面板（Ports 配置表单/列表） |
| 5 | 组件集-9.dsl.json | 主区顶部 5 个图标+文本页签（Convergence Monitor / Report / Simulation response / Manual tuning / Parameter Sensitivity） | 主区 Tab 页签栏 |
| 6 | 容器-373.dsl.json | 主区上部统计条：RMSE 0.768418 / Fit -- / Cost 59.046566 / Iterations 38 | 统计指标条（只读数值） |
| 7 | 容器-19.dsl.json | 1068×24 居中文本「Scalar parameters」，50% 透明度 | 表格分区标签 |
| 8 | 容器-365.dsl.json | 主区大表格：列 Step / Cost / Ki / Kp / Tau / wn / K_brake×5，右侧竖向滚动条，一列有选中单元格高亮（0.7654） | 数据表格（Convergence Monitor 迭代表） |
| 9 | 容器-380.dsl.json | 主区底部通栏消息：「Estimation finished. Ceres termination: CONVERGENCE；…」 | 状态/结果消息条 |
| 10 | 容器-366.dsl.json | 最底部通栏：左「Run stopped / Iteration: 0 / Step: 0」，右「Algorithm progress + 进度条 + competed Iteration 69」 | 底部状态栏 |
| 11 | 容器-392.dsl.json | 居中 1080×760 浮层：标题「Algorithm」+ 关闭图标，Objective / General / Jacobian 三段表单，底部 Cancel / Apply，右侧自有滚动条 | 模态对话框（算法设置表单） |

## 语义组件推断（逐区域）

| 区域 | 结构特征（来自 DSL） | 推断组件 | 置信度 | 备注/TODO |
| --- | --- | --- | --- | --- |
| 容器-1 | INSTANCE `LOGO` + 标题 TEXT + 3 个 `Light/icon/16px/*`（minize/矩形/close） | 标题栏 + 窗口控制按钮（最小化/最大化/关闭） | 高 | Web 化时窗口控制可 mock；图标占位 + TODO |
| 容器-379 | 5×`Light/button/icon/table-icon/default` + TEXT「Language」+ INSTANCE `Light/Input/drop-down/default`（OVTEXT=English，含三角箭头）+ 右侧 2 图标按钮 | 工具栏：图标按钮组 + 语言下拉选择 `<select>` + Run/Stop 按钮 | 高 | 图标语义按截图：新建/导入/保存/设置/刷新 + ▶Run / ■Stop，待 Phase 6 确认 |
| Tab | 4×（INSTANCE 图标 `Icon/General/Tab/{Parameters,Ports,Datasets,EstimateResults}` + TEXT），竖排 | 侧边导航菜单 `<aside>`，激活项=Ports | 高 | 菜单项 → `v-for`；激活态用左边框/背景高亮 |
| 容器-350 | 标题行（`Icon/General/Tab/Ports`+「Ports」）+ 说明 TEXT + 2 组表头（Fit/Port/CSV header）+ 10 行同构行（INSTANCE `选中/未选中` + Port TEXT + 容器367 CSV header 值） | 复选框列表表单：每行 = checkbox（Fit）+ 端口名 + CSV header 可编辑绑定框 | 高 | 行数据 `v-for`；勾选态：有 `编组 39`（对勾图标）=选中，`矩形备份 21` 单独=未选；v_init_kmh 行带清除图标=激活编辑态 |
| 组件集-9 | 5×（INSTANCE 图标 `Icon/General/Tab/*` + TEXT + 分隔矩形） | Tabs 页签组件 + `v-model` 激活态 | 高 | 激活 Tab=Convergence Monitor（由主区内容推断） |
| 容器-373 | 4×（label TEXT + 200px 值容器）：RMSE/Fit/Cost/Iterations | 只读统计指标组（描述列表） | 高 | 值为 mock 数据 |
| 容器-19 | 单行居中 TEXT「Scalar parameters」，opacity 0.5 | 表格分区标题/说明文字 | 中 | 语义弱化文本，作 `<div class="section-label">` |
| 容器-365 | 11 个等宽列 FRAME（各 23–24 子节点：列表头+行单元格）+ `Light/other/scroll-bar` | 数据表格 `<table>`，竖向滚动；单元格可选中高亮 | 高 | 列头 Step/Cost/Ki/Kp/Tau/wn/K_brake×5（K_brake 为设计稿重复列，实际应为不同参数名，TODO 确认）；行数据 `v-for` |
| 容器-380 | 1068×40 单行长 TEXT | 结果消息条（只读，中英混排） | 高 | mock 数据 |
| 容器-366 | 左组：Run/stopped/Iteration/Step + 1px 分隔矩形；右组：Algorithm progress + 200×4 双矩形（轨道+填充）+ competed Iteration 69 | 状态栏 + 进度条 `<progress>` | 高 | 进度条=双矩形叠加，取填充比例 |
| 容器-392 头部 | `LOGO` + TEXT「Algorithm」+ INSTANCE `Light/icon/16px/close` | Dialog 标题栏 + 关闭按钮 | 高 | `Dialog` + `v-model:visible` |
| 容器-392 Objective 段 | label「Objective metric」+ 容器388（INSTANCE `选中/未选中` 圆形双矩形 + TEXT「Sum Squared Error (SSE)」）；「Dynamic performance calibration」+ 方框勾选 + 说明文字 | 单选 radio（SSE，选中）+ 复选 checkbox（已勾选）+ 帮助文本 | 高 | radio/checkbox 形态区分：radio 多一层 14×14 圆形矩形 |
| 容器-392 权重网格 | 画板326/327/328/329：8+4 个「200px label + `Light/Input/normal/activated` 256×24 内嵌数值」行 | 数字输入框表单（两列网格布局） | 高 | Tracking/Curvature/Overshoot/Smoothness/Steady-state weight、Max overshoot、Steady-state tail ratio、Array delta limit、Max iterations、Parameter/Function tolerance |
| 容器-392 Parallel workers | 同上但输入框内含下拉三角 INSTANCE（`icon图标/16px/关闭`+三角形） | 下拉选择 `<select>`（或 InputNumber+下拉） | 中 | 值为「1e-8」疑似设计稿复制错误，TODO 确认合理默认（如 worker 数） |
| 容器-392 General 段 | 「Use numeric Jacobian」方框勾选项 | 复选 checkbox（已勾选） | 高 | |
| 容器-392 Jacobian 段 | 「Jacobian finite-difference base step (h)」输入行 + 「Jacobian difference mode」两个圆选项（Forward difference 选中 / Central difference 未选） | 数字输入框 + radio 组（2 选 1） | 高 | |
| 容器-392 底部 | 画板298「Cancel」（无强调）+ 画板297「Apply」（蓝色强调） | 次要按钮 + 主按钮 | 高 | 截图中位于对话框右下 |
| 容器-392 滚动条 | `Light/other/scroll-bar`（轨道 12×732 / 滑块 8×258） | 对话框内容区竖向滚动 | 高 | 表单超长可滚动，按钮区固定 |

## 复用候选（供 detect_shared + Phase 3 确认）

| 结构 | 本页出现次数 | 疑似跨页复用 | 建议组件名 |
| --- | --- | --- | --- |
| 标题栏（logo+标题+窗口控制） | 1 | 全部 16 画板 | `AppTitleBar` |
| 工具栏（图标按钮+Language 下拉+Run/Stop） | 1 | 全部画板 | `AppToolBar` |
| 左侧导航（4 项图标+竖排文本） | 1 | 全部画板 | `SideNav` |
| 主区 Tab 页签栏（5 页签） | 1 | EstimateResults 相关画板 | `MainTabs` |
| 底部状态栏（Run 状态+Algorithm progress） | 1 | 全部画板 | `AppStatusBar` |
| 图标按钮 `Light/button/icon/table-icon/default` | 8 | 工具栏/各面板 | `IconButton` |
| 复选框/单选 INSTANCE `选中/未选中` | 15+ | 弹窗表单、Ports 面板 | `AppCheckbox` / `AppRadio` |
| 输入框 `Light/Input/normal/activated`（label+256px 输入） | 12 | Algorithm 弹窗及其他设置弹窗 | `FormField`（label+input） |
| 下拉 `Light/Input/drop-down/default` | 2 | 工具栏、Parallel workers | `AppSelect` |
| 表格行（checkbox+port+CSV header） | 10 | Ports 面板 | `PortRow` |
| 迭代表格列（列头+单元格） | 11 列 | Convergence Monitor 各状态画板 | `ConvergenceTable` |

## 交互推断（规则见 references/interaction-inference.md）

| 触发元素 | 推断事件 | 目标（路由/页内状态） | 置信度 | 依据 |
| --- | --- | --- | --- | --- |
| Algorithm 弹窗 × 关闭图标 | click | 页内状态 `algorithmDialogVisible=false` | 高 | 弹窗标准关闭惯例；其他画板存在无弹窗的同一窗口状态 |
| Cancel 按钮 | click | 关闭弹窗，不保存（`visible=false`） | 高 | 按钮文案即事件 |
| Apply 按钮 | click | 校验并保存算法配置（store）→ 关闭弹窗 | 高 | 按钮文案 + 主按钮强调色 |
| Objective metric 单选（SSE…） | change | 表单字段 `objectiveMetric` | 高 | radio 结构明确 |
| Dynamic performance calibration / Use numeric Jacobian 复选框 | change | 表单布尔字段；勾选后启用对应权重输入组 | 中 | 勾选与权重输入的联动为视觉惯例推断 |
| Jacobian difference mode（Forward/Central） | change | 表单字段 `jacobianMode` | 高 | radio 二选一结构 |
| Parallel workers 下拉 | change | 表单字段 `parallelWorkers` | 中 | 带下拉三角但值为「1e-8」，控件类型存疑 |
| 工具栏 ▶（Run）按钮 | click | 启动估计运行 → 状态栏 Run stopped→running，进度条推进 | 中 | 视觉惯例（播放图标）+ 状态栏有 Run 状态 |
| 工具栏 ■（Stop）按钮 | click | 停止运行 → 状态栏回 stopped | 中 | 与 ▶ 成对出现 |
| Language「English」下拉 | change | 切换 UI 语言（i18n locale） | 高 | `Light/Input/drop-down` 实例 + Language 文案 |
| 左侧导航 Parameters/Ports/Datasets/EstimateResults | click | 切换左侧面板内容（页内状态 `activeNav`） | 高 | 跨画板仅导航高亮+面板内容变化 |
| 主区 5 个 Tab 页签 | click | 切换主区内容（页内状态 `activeMainTab`，非路由） | 高 | 交互规则：Tab 页签多画板仅内容区不同 |
| Ports 行 Fit 复选框 | change | 切换该 port 启用状态 | 高 | 表头「Fit」+ checkbox 结构 |
| Ports 行 CSV header 值框 | click | 进入编辑态绑定 CSV 列（出现清除图标/下拉） | 高 | 说明文案「bind each one to a CSV header (click to edit)」+ 激活行带清除图标 |
| 迭代表格单元格 | click | 选中单元格高亮（如 0.7654） | 低 | 截图有单个高亮单元格，用途不明，TODO 确认 |
| 标题栏窗口控制（最小化/最大化/关闭） | click | 桌面窗口操作 | 低 | Web 化无对应行为，建议 mock + TODO |

## 数据识别（mock 数据 → 响应式数据）

| 设计稿中的内容 | 数据字段 | 类型 |
| --- | --- | --- |
| 迭代表格行（Step 1–9，Cost/Ki/Kp/Tau/wn/K_brake 各列数值） | `convergenceRows` | `Array<{step:number, cost:number, ki:number, kp:number, tau:number, wn:number, kBrake:number, ...}>` |
| 表格列头 Step/Cost/Ki/Kp/Tau/wn/K_brake×5 | `convergenceColumns` | `string[]`（K_brake 重复列待确认真实参数名） |
| RMSE 0.768418 / Fit -- / Cost 59.046566 / Iterations 38 | `runStats` | `{rmse:number, fit:string|null, cost:number, iterations:number}` |
| 「Estimation finished. Ceres termination: CONVERGENCE；…SSE=60.3646.」 | `statusMessage` | `string` |
| Run / stopped / Iteration: 0 / Step: 0 | `runStatus` | `{state:'stopped', iteration:number, step:number}` |
| Algorithm progress 进度条 +「competed Iteration 69」 | `algorithmProgress` | `{percent:number, label:string}` |
| Input ports 行（T1、T1、v_init_kmh，均勾选） | `inputPorts` | `Array<{fit:boolean, port:string, csvHeader:string}>` |
| Out ports 行（a_imu_x_hat/ax_vehicle/ax_motion/ax_free/v_init_kmh/pitch_hat/v_init_kmh，仅 ax_motion 勾选） | `outPorts` | `Array<{fit:boolean, port:string, csvHeader:string}>` |
| Algorithm 表单值（SSE、calibration 勾选、weights 1/0/0/0/0/0/0.2/1、Max iterations 220、tolerances 1e-8、h 0.00001、Forward difference） | `algorithmForm` | `object`（默认值即设计稿值） |
| 语言「English」 | `locale` | `string` |
| 左导航 4 项、主 Tab 5 项 | `navItems` / `mainTabs` | `string[]`（含图标名） |

## 存疑清单

- 迭代表格 5 个重复「K_brake」列：设计稿复制占位，真实参数列名待确认（Phase 6/7 用 mock 列名 + TODO）。
- Parallel workers 控件：带下拉三角但值为「1e-8」，下拉还是数字输入待确认，默认按 `<select>` 实现 + TODO。
- 表格单元格高亮（0.7654）的交互含义不明，默认实现为可选中样式 + TODO。
- 窗口控制按钮在 Web 化中无实际行为，建议仅展示或省略 + TODO。
- 弹窗 Cancel/Apply 在 DSL 中位于顶部节点序，但截图为右下角 footer，按截图实现为固定底部按钮区。
- 状态栏文案「competed Iteration 69」疑为「completed」笔误，保留原文案，不擅自修正。

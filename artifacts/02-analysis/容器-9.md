# 02 - 页面分析：容器-9

> 由 AI 在 Phase 2 填写。**一次只分析一个画板**，依据：整页截图 + 逐区域 DSL。
> 数据来源：`artifacts/raw/参数估计/容器-9.png`、`容器-9.dsl.json`、`regions/容器-9/*.dsl.json`

## 基本信息

- 页面 / 画板：容器-9　GUID：19:1867
- 页面角色推断（列表 / 详情 / 表单 / 登录…）：同一应用窗口「Param Estimation Toolbox - HAS Behavior」的一个**页内状态**：左侧导航选中 **Ports**，主区 Tab 激活 **Parameter Sensitivity**（填充态，Initial point / Best point 两张敏感度表格有数据），无弹窗；底部状态栏 Run stopped、Algorithm progress 满格（competed Iteration 69）。置信度：高

## 区域划分

| # | 区域（DSL 文件名） | 视觉描述 | 语义角色 |
| --- | --- | --- | --- |
| 1 | 容器-1.dsl.json | 顶部通栏：左侧 BEH 蓝色 LOGO + 加粗标题「Param Estimation Toolbox - HAS Behavior」，右侧 3 个 16px 小图标 | 窗口标题栏（Header） |
| 2 | 容器-379.dsl.json | 第二行通栏：左侧 5 个图标按钮 + 「Language」标签 + 「English」下拉框，右侧 2 个图标按钮（▶ / ■） | 工具栏（Toolbar） |
| 3 | Tab.dsl.json | 左侧窄栏纵向 4 项（图标+竖排文字）：Parameters / Ports（高亮选中）/ Datasets / EstimateResults | 左侧导航菜单（SideNav） |
| 4 | 容器-380.dsl.json | 导航右侧的面板：标题「Datasets」+ 刷新/删除图标，表头 NAME / Type / Time，4 行 Result 记录 | 数据集列表面板（侧栏表格） |
| 5 | 组件集-9.dsl.json | 主区顶部等距 5 个页签：Convergence Monitor / Report / Simulation response / Parameter Sensitivity（下划线高亮）/ Manual tuning | 主区 Tab 页签（Tabs） |
| 6 | 容器-381.dsl.json | 主区内容：「Perturbation ratio: 0.01」、Initial point 汇总行 + 敏感度表格（带纵向滚动条）、Best point 汇总行 + 同构表格（带滚动条） | 主内容区（Parameter Sensitivity 结果表格 ×2） |
| 7 | 容器-366.dsl.json | 底部通栏：左「Run stopped | Iteration: 0 | Step: 0」，右「Algorithm progress + 蓝色进度条（满）+ competed Iteration 69」 | 状态栏（StatusBar） |

## 语义组件推断（逐区域）

| 区域 | 结构特征（来自 DSL） | 推断组件 | 置信度 | 备注/TODO |
| --- | --- | --- | --- | --- |
| 容器-1 标题栏 | INSTANCE `LOGO`（渐变矢量）+ TEXT 16px Bold「Param Estimation Toolbox - HAS Behavior」，右侧 INSTANCE `Light/icon/16px/close备份`（Icon/installer/minize）、`...备份 2`（maxmize）、`Light/icon/16px/close`（Icon/installer/close） | Header：logo + 标题 + 窗口控制按钮组（minimize/maximize/close） | 高 | 组件名 refsIndex 明写 minimize/maxmize/close；桌面壳窗口控制按钮，Web 化时可保留为装饰或实现关闭弹层提示，TODO |
| 容器-379 工具栏 | 多个 INSTANCE `Light/button/icon/table-icon/default*`，图标 refs 含 `file_import`、`file_export`、`View/save`、`View/run`、`Menu/refresh`、`+`、`expand`；TEXT「Language」+ INSTANCE `Light/Input/drop-down/default`（内含 TEXT「English」+ 三角形下拉箭头 `icon图标/16px/关闭`） | 图标按钮组 + 语言下拉选择框 | 高 | 「English」下拉有箭头三角，按 Dropdown 实现；左侧图标按钮依次约：导入/导出/保存/另存(或新建)/刷新，具体语义仅图标可辨，置信度中，TODO 标注 |
| 容器-379 右侧 | `Light/button/icon/table-icon` ×2（含 `Icon/General/View/run`） | Run / Stop 控制按钮 | 中 | 截图为 ▶、■ 两图标，对应启动/停止估计运行 |
| Tab.dsl.json 左侧导航 | INSTANCE `Tab`，组件集属性1 变体 = Ports；4 个「图标（Icon/General/Tab/*）+ TEXT」纵向菜单项，高 844 | SideNav 菜单，激活项 = Ports | 高 | 组件集 4:1383 的变体枚举即菜单全集：Parameters/Ports/Datasets/EstimateResults |
| 容器-380 数据集面板 | 标题「Datasets」+ 刷新图标 + 垃圾桶图标；表头 TEXT「NAME」「Type」「Time」；4 个同构「组件集 3」行：复选框 + 「Result #N」+「Auto」+ 时间 + 行尾 2 个操作图标（详情○/删除🗑） | 带复选的数据列表（v-for 表格/列表） | 高 | Result #4 复选框为勾选态（蓝色），其余未选；4 行静态结构同构，必须 v-for 数据驱动 |
| 组件集-9 主区 Tab | INSTANCE `组件集 9`，组件集「组件集 8」变体 = Parameter Sensitivity；5 个「图标+TEXT」页签，间以竖向分隔矩形 | Tabs 页签，激活项 = Parameter Sensitivity（蓝色下划线） | 高 | 变体枚举即 Tab 全集：Convergence Monitor/Report/Simulation response/Parameter Sensitivity/Manual tuning |
| 容器-381 内容区 | TEXT「Perturbation ratio: 0.01」；「Initial point」加粗 + 灰色汇总文本；4 列同构单元格（Parameter / ΔSSE (train) / ΔSSE (validation) / ΔSSE (total)）×11 行 + INSTANCE `Light/other/scroll-bar`；「Best point」+ 同构第二表 + 滚动条 | 参数说明行 + 数据表格 ×2（各带纵向滚动） | 高 | tau_brake 行蓝色高亮（hover/选中态）；两表结构完全同构，应抽成一个 Table 组件复用；列宽固定、数值右对齐 |
| 容器-366 状态栏 | 左组 TEXT「Run」「stopped」+ 1px 竖分隔线 +「Iteration: 0」+ 分隔线 +「Step: 0」；右组「Algorithm progress」+ 组合 17（灰底 200×4 圆角矩形 + 蓝色 200×4 圆角矩形叠加）+「competed Iteration 69」 | StatusBar：运行状态文本 + 进度条（ProgressBar） | 高 | 蓝色进度矩形宽度=灰色底宽 → 进度 100%；进度条实现为 width 百分比绑定 |

## 复用候选（供 detect_shared + Phase 3 确认）

| 结构 | 本页出现次数 | 疑似跨页复用 | 建议组件名 |
| --- | --- | --- | --- |
| 标题栏（logo+标题+窗口控制） | 1 | 全部 16 画板同位置同构 | `AppTitleBar` |
| 工具栏（图标按钮组+Language 下拉+Run/Stop） | 1 | 全部画板复用 | `AppToolbar` |
| 左侧导航 Tab（组件集 4:1383，4 变体） | 1 | 全部画板复用，仅激活变体不同 | `SideNav` |
| 主区 Tab 页签（组件集 8，5 变体） | 1 | 全部画板复用，仅激活变体不同 | `MainTabs` |
| 状态栏（Run 状态 + Algorithm progress 进度条） | 1 | 全部画板复用，文案/进度不同 | `StatusBar` |
| 敏感度表格（Initial point / Best point） | 2 | 页内复用 ×2；其他画板表格同构 | `SensitivityTable` / `DataTable` |
| 数据集行（复选框+名称+Type+Time+操作图标） | 4 | 其他画板 Datasets 面板复用 | `DatasetRow`（v-for） |
| 图标按钮 `Light/button/icon/table-icon` | 7+ | 工具栏/面板头/行内操作通用 | `IconButton` |

## 交互推断（规则见 references/interaction-inference.md）

| 触发元素 | 推断事件 | 目标（路由/页内状态） | 置信度 | 依据 |
| --- | --- | --- | --- | --- |
| 左侧导航 Parameters / Ports / Datasets / EstimateResults | click → 切换左侧面板内容 | 页内状态 `activeNav` | 高 | 导航是 4 变体组件集；各画板仅激活项与面板内容不同（本画板 Ports 激活却显示 Datasets 面板，见存疑 1） |
| 主区 Tab 5 页签 | click → 切换主区内容 | 页内状态 `activeMainTab` | 高 | Tab 为 5 变体组件集，画板间仅内容区不同，规则「Tab 页签多画板 = 页内切换」 |
| 数据集行复选框 | click → 勾选/取消，选中结果驱动主区数据 | 页内状态 `selectedResults` | 高 | Result #4 勾选且主区有数据，其余未勾选；勾选行与填充态强相关 |
| 数据集行尾删除图标（🗑）/ 面板头垃圾桶 | click → 删除该条/所选数据集 | 页内状态（列表数据删除） | 中 | 图标语义明确但无确认弹窗画板佐证，Phase 7 默认直接删除 + TODO |
| 数据集行尾圆形图标（○） | click → 查看/定位该结果详情 | 页内状态 | 低 | 仅图标可辨，无语义命名，合理默认实现 + TODO |
| 面板头刷新图标 | click → 刷新数据集列表 | 页内状态（mock 重取） | 中 | 图标组件名含 refresh；视觉惯例 |
| 工具栏 Language「English」下拉 | click → 展开语言选项，切换语言 | 页内状态 `language`（i18n） | 中 | `Light/Input/drop-down` 实例 + 三角箭头；可选语言项无画板佐证，默认 English/中文 + TODO |
| 工具栏导入/导出/保存/刷新图标按钮 | click → 文件导入/导出/保存/重置 | 页内动作（mock） | 低 | 图标组件名 file_import/file_export/save 可辨，但具体行为无画板佐证，TODO |
| 工具栏 ▶ Run / ■ Stop | click → 启动/停止估计算法，更新状态栏 | 页内状态 `runState` | 高 | 状态栏「Run stopped」与按钮互斥状态对应；`Icon/General/View/run` 命名佐证 |
| 标题栏 minimize/maximize/close | click → 窗口控制 | 页内动作（Web 端装饰/mock） | 低 | 桌面窗口语义，Web 化无对应行为，TODO |
| 表格行（如 tau_brake 高亮） | hover/click → 行高亮 | 页内状态 `activeRow` | 中 | 截图中 tau_brake 行为蓝色高亮态 |
| 状态栏进度条 | 无交互（数据驱动展示） | — | 高 | 纯展示组件 |

## 数据识别（mock 数据 → 响应式数据）

| 设计稿中的内容 | 数据字段 | 类型 |
| --- | --- | --- |
| 数据集列表 Result #1–#4 / Auto / 11:28:34、10:21:54 | `datasets: [{ id, name, type, time, checked }]` | Array<Object> |
| 「Datasets」面板标题 | 静态文案 | string |
| 主区 Tab 名 Convergence Monitor / Report / Simulation response / Parameter Sensitivity / Manual tuning | `mainTabs: string[]`（激活 `activeMainTab='Parameter Sensitivity'`） | Array<string> |
| 左导航 Parameters / Ports / Datasets / EstimateResults | `navItems: string[]`（激活 `activeNav='Ports'`） | Array<string> |
| 「Perturbation ratio: 0.01」 | `perturbationRatio` | number (0.01) |
| Initial point (Train=4117.467967616531, Validation=0, Total=4117.467967616531) | `initialPoint: { train, validation, total }` | Object<number> |
| Best point (Train=59.046565695501414, Validation=0, Total=59.046565695501414) | `bestPoint: { train, validation, total }` | Object<number> |
| 表格行 K_drive / K_brake / C_v / C_roll / tau_drive / tau_brake / v_eps / K_ddrive / K_dbrake / v_eps / K_ddrive ×（train/validation/total 三组数值，如 438.3616584226363 / 0 / 438.3616584226363 等） | `sensitivityRows: [{ parameter, dSseTrain, dSseValidation, dSseTotal }]`（Initial/Best 两表数据相同，各一份） | Array<Object> |
| 状态栏「Run」「stopped」「Iteration: 0」「Step: 0」 | `runState`、`iteration`、`step` | string / number / number |
| 「Algorithm progress」+ 进度 100% +「competed Iteration 69」（原文拼写 competed，保留） | `progress` (0–100)、`progressText` | number / string |
| 「Language」「English」 | `language` | string |

## 存疑清单

- 左导航激活项为 **Ports**，但下方显示的是 **Datasets** 列表面板（内容与其他画板的 Datasets 面板相同）。可能是设计稿状态不一致，或该面板与导航项非一一对应。Phase 7 默认：面板内容随导航切换，TODO 确认。
- 工具栏左侧 5 个图标按钮的具体语义（导入/导出/保存/新建/刷新？）仅图标可辨，Phase 7 以合理默认 + TODO。
- 数据集行尾圆形图标语义不明（查看/详情？），低置信度，TODO。
- 两张敏感度表格 Initial point 与 Best point 数据在设计稿中完全相同，mock 数据可复用同一份；validation 列全 0，与设计稿一致。
- 标题栏窗口控制按钮在 Web 端无真实行为，按装饰实现 + TODO。
- 「competed Iteration 69」疑为「completed」笔误，按原稿保留文案。

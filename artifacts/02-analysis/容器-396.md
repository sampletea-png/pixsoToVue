# 02 - 页面分析：容器-396

> 由 AI 在 Phase 2 填写。**一次只分析一个画板**，依据：整页截图 + 逐区域 DSL。
> 数据来源：`artifacts/raw/参数估计/容器-396.png`、`容器-396.dsl.json`、`regions/容器-396/*.dsl.json`

## 基本信息

- 页面 / 画板：容器-396　GUID：63:4435
- 页面角色推断（列表 / 详情 / 表单 / 登录…）：**应用主窗口 +「Parameters」参数管理弹窗打开状态（填充态）**。弹窗内为参数选择/编辑表格（表单+列表混合）；背景为 Parameters 导航页（左侧参数列表面板 + 右侧迭代数值表）。置信度：高

> 状态差异判断：相对应用骨架，本画板左侧导航激活项为 **Parameters**，主区被「Parameters」模态弹窗（1280×720，位于 54,116）覆盖；弹窗表格已填充 9 行参数数据（填充态），底部状态栏显示 `Run stopped / Iteration: 0 / Step: 0`，右下 `Algorithm progress` 进度条 + `competed Iteration 69`（设计稿原文拼写如此）。

## 区域划分

| # | 区域（DSL 文件名） | 视觉描述 | 语义角色 |
| --- | --- | --- | --- |
| 1 | 容器-1.dsl.json | 顶部通栏：BEH logo + 窗口标题 + 右侧 3 个窗口控制图标 | 标题栏（桌面窗口 chrome） |
| 2 | 容器-379.dsl.json | 标题栏下方通栏：左侧 5 个图标按钮 + Language/English 下拉，右侧 2 个图标按钮（▶/■） | 工具栏 |
| 3 | Tab.dsl.json | 左侧窄竖栏，4 项图标+竖排文字（Parameters 高亮） | 侧边栏导航（垂直 Tab） |
| 4 | 容器-350.dsl.json | 导航右侧 328px 面板：表头（Parameters + 图标按钮）+ NAME/Current Value 列 + 9 行参数 | 参数列表面板（背景，被弹窗部分遮挡） |
| 5 | 容器-397.dsl.json | 右上 1068×412 纯白空矩形（几乎全部被弹窗遮挡） | 空白占位面板（疑似图表区空态，TODO 待确认） |
| 6 | 容器-365.dsl.json | 右下 1068×428 数值表格（11 列×19 行）+ 纵向滚动条，一行高亮 | 迭代历史数据表（背景，右侧边缘可见） |
| 7 | 容器-366.dsl.json | 底部通栏：Run stopped \| Iteration: 0 \| Step: 0 … Algorithm progress + 进度条 + competed Iteration 69 | 状态栏 |
| 8 | 容器-398.dsl.json | 居中 1280×720 白色浮层：标题栏 + 按钮组 + 数组条目 + 参数大表 + 底栏 | **Parameters 模态弹窗（本画板主体）** |

## 语义组件推断（逐区域）

### 弹窗 容器-398（1280×720，本画板核心）

| 区域 | 结构特征（来自 DSL） | 推断组件 | 置信度 | 备注/TODO |
| --- | --- | --- | --- | --- |
| 容器 2（弹窗头部 1232×40） | LOGO 实例(comp 4:1489) + 文本 `Parameters` + `Light/icon/16px/close` 实例(comp 4:1511) | Dialog 标题栏 + 关闭按钮 | 高 | 关闭按钮 → 关闭弹窗 |
| 容器 385（251×24） | 两个圆角矩形按钮：`Add parameters`（118×24，截图蓝色强调）、`Clear parameters`（125×24，灰底） | primary 按钮 + secondary 按钮 | 高 | |
| 容器 437（1232×64，2 个同构「组件集 3」行） | 每行：名称文本 + 描述文本 + 右侧 `Detailed edit`（蓝底 108×24）+ `Remove table`（灰底 108×24） | 已选数组条目列表（v-for） | 高 | 行 1：`.pitch_model_1.Param` / `1D array · 50 · 0/50 selected`；行 2：`.pitch_model_1.Param_1` / `2D array · 10 x 10 · 0/100 selected` |
| 容器 365（弹窗内表格 1232×516，11 列） | 列结构：画板61(62px ACTION) + 画板309(60px FIX) + 9 个文本列；表头行 24px + 数据行 32px，共 9 行数据 + 10 行空行占位 | 可编辑数据表格（v-for 行 + 列定义） | 高 | 列：ACTION / FIX / PARAMETER / CURRENT / INITIAL / FIXED / UNIT / DESCRIPTION / LOWER BOUND / UPPER BOUND / SCALE / Physical meaning (optional) |
| ACTION 列单元格 | 「组合 15」16×16 矢量组（垃圾桶造型）每行一个 | 行删除图标按钮 | 高 | 矢量图标 → 占位 + TODO 图标库 |
| FIX 列单元格 | INSTANCE `选中/未选中`（comp 4:1461）每行一个 | 复选框（固定参数开关） | 高 | 9 行均为未选中态 |
| 高亮行（tau_brake 行） | 截图中该行整行浅蓝高亮，且 INITIAL/FIXED、LOWER BOUND、UPPER BOUND、SCALE 单元格渲染为带边框输入框 | 行内编辑（inline edit）输入框 | 中 | 其余行同列显示为纯文本 → 点击进入编辑态的推断，TODO 交互待确认 |
| UNIT / DESCRIPTION 列 | 全部 9 行均为 `—` | 占位文本（无数据态） | 高 | 可视为可编辑单元格，当前为空 |
| 容器 366（弹窗底栏 1280×24） | 左对齐文本 `Selected estimation parameters: 11` | 弹窗统计信息栏 | 高 | 11 = 派生数据（选中参数计数），注意与 9 行表格不一致（存疑，见下） |

### 应用骨架区域

| 区域 | 结构特征（来自 DSL） | 推断组件 | 置信度 | 备注/TODO |
| --- | --- | --- | --- | --- |
| 容器-1 标题栏 | LOGO 实例 + 文本 ` Param Estimation Toolbox - HAS Behavior` + `Icon/installer/minize`、`Light/icon/16px/close备份 2`（最大化）、`Icon/installer/close` | 窗口标题栏 + 最小化/最大化/关闭按钮 | 高 | 桌面 chrome，前端可仅做视觉还原 |
| 容器-379 工具栏 | 左组 5 个 `Light/button/icon/table-icon/default` 图标按钮（含 +、save 等）+ 文本 `Language` + `Light/Input/drop-down/default` 实例（文本 `English` + 下拉三角）；右组 2 个图标按钮（截图 ▶ 运行 / ■ 停止） | 工具栏：图标按钮组 + 语言下拉选择 + 运行/停止按钮 | 高 | 图标语义部分未命名，TODO 待确认各图标功能 |
| Tab 左侧导航 | INSTANCE `Tab`（comp 4:1384），4 个子项各含 `Icon/General/Tab/{Parameters,Ports,Datasets,EstimateResults}` 图标 + 竖排文本；Parameters 项高亮（截图蓝色左边条） | 垂直侧边栏导航（4 项，Parameters 激活） | 高 | 文本为竖排（旋转），实现时可用 writing-mode |
| 容器-350 参数面板 | 表头行（Parameters 图标 + 文本 `Parameters` + `Icon/General/Tab/file_export` 图标按钮）+ 列头 `NAME` / `Current Value` + 9 个同构行（名称 + 数值两文本） | 参数列表面板（v-for） | 高 | 表头右侧图标按钮 DSL 命名为 file_export，截图形似铅笔，TODO 待确认是导出还是编辑 |
| 容器-365 背景数值表 | 11 列（48px 序号列 + 10×100px 数据列）×19 行单元格 + `Light/other/scroll-bar` 滚动条（12×216，滑块 8×60）；列头含 Kp / Ki / Tau / wn / K_brake / Cost / Step 等，数据为小数 | 迭代历史数据表 + 纵向滚动条 | 中 | 表头与列的对应关系部分被遮挡，TODO 列顺序待确认 |
| 容器-397 空白面板 | 1068×412 纯白矩形，无任何子节点 | 空白占位区（疑似图表空态） | 低 | TODO: 语义待确认，先还原为空白容器 |
| 容器-366 状态栏 | 左组三段文本（`Run` `stopped` \| `Iteration: 0` \| `Step: 0`，1px 分隔线）+ 右组 `Algorithm progress` + 200×4 进度条（双层矩形）+ `competed Iteration 69` | 状态栏 + 进度条组件 | 高 | `competed` 为设计稿原文拼写（疑为 completed/ competed 笔误），保留原文 |

## 复用候选（供 detect_shared + Phase 3 确认）

| 结构 | 本页出现次数 | 疑似跨页复用 | 建议组件名 |
| --- | --- | --- | --- |
| 标题栏（logo+标题+窗口控制） | 1 | 全部 16 画板共有 | `AppTitleBar` |
| 工具栏（图标按钮组 + Language 下拉 + 运行/停止） | 1 | 全部画板共有 | `AppToolbar` |
| 左侧垂直导航 Tab | 1 | 全部画板共有 | `SideNav` |
| 底部状态栏（Run/Iteration/Step + Algorithm progress） | 1（弹窗内另有相似底栏变体） | 全部画板共有 | `StatusBar` |
| 图标按钮 `Light/button/icon/table-icon/default` | 工具栏 7 + 面板表头 1 | 跨画板高频 | `IconButton` |
| 按钮（蓝 primary / 灰 secondary，高 24） | 弹窗内 4（Add/Clear/Detailed edit×2/Remove table×2） | 跨画板 | `AppButton` |
| 数据表格（Header 行 + 32px 数据行 + 列容器） | 2（弹窗参数表、背景迭代表） | 多画板（Datasets/EstimateResults 页大概率同款） | `DataTable` |
| 复选框 `选中/未选中`(comp 4:1461) | 9 | 跨表格复用 | `AppCheckbox` |
| 滚动条 `Light/other/scroll-bar` | 1 | 跨画板 | 建议用原生/CSS 滚动条替代 |
| 数组条目行（名称+描述+双按钮） | 2 | 弹窗内 v-for | `ParamArrayItem` |

## 交互推断（规则见 references/interaction-inference.md）

| 触发元素 | 推断事件 | 目标（路由/页内状态） | 置信度 | 依据 |
| --- | --- | --- | --- | --- |
| 弹窗右上 close 图标 | click → 关闭弹窗 | 页内状态 `dialogVisible=false` | 高 | 关闭图标语义明确；其他画板存在无弹窗的同骨架状态（元素同位置变化→页内状态） |
| `Add parameters` 按钮 | click → 打开参数选择（添加）界面 | 页内状态/次级弹窗，TODO 待确认 | 中 | 按钮文案即事件；16 画板中疑似有对应添加态画板 |
| `Clear parameters` 按钮 | click → 清空参数表格 | 页内状态（表格数据清空 → 空态） | 中 | 文案即事件；存在空态画板的可能性 |
| 数组条目行 `Detailed edit` | click → 打开该数组的详细编辑视图 | 页内状态/次级弹窗，TODO 待确认 | 中 | 文案即事件 |
| 数组条目行 `Remove table` | click → 移除该数组条目 | 页内状态（列表项删除） | 中 | 文案即事件 |
| 表格 ACTION 列垃圾桶图标 | click → 删除该参数行 | 页内状态（行删除） | 高 | 删除图标视觉惯例 + 每行一个 |
| 表格 FIX 列复选框 | toggle → 固定/解固定该参数 | 页内状态（行数据 `fixed` 字段） | 高 | 复选框组件实例（选中/未选中）语义明确 |
| 表格单元格（INITIAL/FIXED、LOWER BOUND、UPPER BOUND、SCALE、Physical meaning） | click → 进入行内编辑 | 页内状态（编辑态行） | 中 | 高亮行（tau_brake）这些单元格渲染为输入框，其余行为纯文本 |
| 左侧导航 Parameters/Ports/Datasets/EstimateResults | click → 切换主区页面 | 页内 Tab 状态（非路由，整应用单窗口） | 高 | 16 画板为同窗口不同状态，导航项为组件变体（属性 1=Parameters/Ports/…） |
| 工具栏右侧 ▶ / ■ 按钮 | click → 运行 / 停止估计算法 | 页内状态（运行态，联动状态栏 `Run stopped`） | 中 | 截图 play/stop 图标 + 状态栏 Run 状态呼应 |
| 工具栏 `English` 下拉 | click → 展开语言选项并切换 | 页内状态（locale） | 中 | `Light/Input/drop-down` 实例 + Language 标签 |
| 窗口控制按钮（minimize/maximize/close） | click → 窗口最小化/最大化/关闭 | 不实现（桌面 chrome），仅视觉还原 | 低 | 视觉惯例；Web 端无对应行为 |
| 参数面板（容器-350）行 | click → 选中参数/与弹窗联动 | 页内状态，TODO 待确认 | 低 | 面板被弹窗遮挡，缺少跨画板证据 |
| 参数面板表头图标按钮（file_export） | click → 导出（或编辑）参数 | 页内状态/下载，TODO 待确认 | 低 | DSL 命名 file_export 与截图铅笔造型不一致 |
| 背景迭代表滚动条 | 滚动 → 查看更多行 | 页内状态 | 高 | scroll-bar 实例 + 19 行超出可视区 |

## 数据识别（mock 数据 → 响应式数据）

| 设计稿中的内容 | 数据字段 | 类型 |
| --- | --- | --- |
| 弹窗参数表 9 行：K_drive / Kbrake / Cv / C_roll / tau_drive / tau_brake / v_eps / K_ddrive / K_dbrake 及各自 CURRENT（1.025397、0.765554、0.000151、0.209244、0.124356、0.006636、0.021103、-0.000535、0.002793）、INITIAL/FIXED、LOWER/UPPER BOUND、SCALE、`—`（UNIT/DESCRIPTION）、`pitch model Stage-1 longitudinal parameter` | `paramList: Array<{ name, current, initial, unit, description, lowerBound, upperBound, scale, meaning, fixed }>` | mock 数组（v-for） |
| 数组条目 2 行：`.pitch_model_1.Param`（`1D array · 50 · 0/50 selected`）、`.pitch_model_1.Param_1`（`2D array · 10 x 10 · 0/100 selected`） | `arrayList: Array<{ name, dimInfo, selectedInfo }>` | mock 数组（v-for） |
| `Selected estimation parameters: 11` | `selectedCount`（派生计数） | computed（设计稿为静态 11，与 9 行不一致，见存疑） |
| 左侧参数面板 9 行 NAME/Current Value（K_drive 1.025397、Kbrake 0.765554、C_v、C_roll、tau_drive、tau_brake、`v_e'p's`、K_ddrive、k_dbrake；数值列大量重复 1.025397/0.765554） | `sideParamList: Array<{ name, value }>` | mock 数组；注意 `v_e'p's`、`k_dbrake` 为设计稿原文拼写 |
| 背景迭代表数值（列头 Kp/Ki/Tau/wn/K_brake/Cost/Step…，数据 0.9334、0.7854、0.8892、0.7654、0.9821、0.598、0.678、11.34、0.3456 等，约 19 行） | `iterationRows: Array<Record<string, number|string>>` | mock 数组（v-for） |
| 状态栏 `Run` / `stopped` / `Iteration: 0` / `Step: 0` / `Algorithm progress` / `competed Iteration 69` | `runStatus`、`iteration`、`step`、`progressText` | mock 响应式状态 |
| 窗口标题 ` Param Estimation Toolbox - HAS Behavior`、语言 `English` | `appTitle`、`locale` | 常量/配置 |

## 存疑清单

- 弹窗底栏 `Selected estimation parameters: 11` 与表格仅 9 行不一致（可能含数组条目中的选中项计数口径不同）→ Phase 7 以 computed 计数实现 + `TODO: 计数口径待确认`。
- 容器-397（1068×412 纯白空矩形）语义不明：疑似图表区空态占位 → 先还原为空白容器 + `TODO: 语义待确认`。
- 背景迭代表（容器-365）列头与列的精确对应关系、首列（48px）内容被弹窗遮挡无法确认 → Phase 6 按 Kp/Ki/Tau/wn/… 通用列实现 + TODO。
- 工具栏左组 5 个图标按钮中除 +、save 外其余图标语义未命名（DSL 均为 table-icon/default 占位）→ 图标占位 + TODO。
- 参数面板表头图标按钮 DSL 命名 `file_export` 但截图形似铅笔（编辑）→ 默认按导出实现 + `TODO: 交互待确认`。
- 状态栏 `competed Iteration 69` 疑为 `completed` 笔误；`v_e'p's`、`k_dbrake`/`K_dbrake` 大小写不一致——均按设计稿原文保留。
- 弹窗无遮罩层节点（背景未变暗），是否为模态仅靠视觉层级推断 → 按模态 Dialog 实现（中置信度）。
